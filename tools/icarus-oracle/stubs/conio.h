// Stub for MSVC <conio.h>, included by the out-of-scope oracle Interpreter.cpp
// (fixture-generator only). getch() is referenced only by the include comment,
// never called, so this is intentionally empty.
#pragma once
