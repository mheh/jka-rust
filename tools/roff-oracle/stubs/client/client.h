// roff-oracle stub — RoffSystem.cpp includes ../client/client.h (line 5). Under
// the WinDed DEDICATED build (ROFF-D3) every client reference in RoffSystem.cpp
// (cgvm, CG_* getters, CG_ROFF_NOTETRACK_CALLBACK) sits inside `#ifndef DEDICATED`
// blocks and compiles OUT, so this header needs to declare nothing. oracle/
// untouched.
#ifndef ROFF_ORACLE_CLIENT_H
#define ROFF_ORACLE_CLIENT_H
#endif
