// Stub server.h — RM_Manager.cpp opens it, but on the DEDICATED live path
// (LoadMission early-out) it needs no SV_* surface; the common services come
// from qcommon.h. oracle never edited (§18).
#pragma once
#include "../qcommon/qcommon.h"
