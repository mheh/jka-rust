// terrainmap-oracle stub: cm_draw.h includes this on the non-linux arm, where
// the real build pulls in the Win32 types. exe_headers.h already declares the
// one type it needs (POINT).
#pragma once
