[package]
name = "sp_uishared"
version = "0.1.0"
edition = "2021"

[lib]
name = "sp_uishared"

[dependencies]
native_types = { path = "../../native/types" }
sp_qshared = { path = "../qshared" }
sp_bg = { path = "../bg" }
