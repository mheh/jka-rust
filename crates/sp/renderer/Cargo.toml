[package]
name = "sp_renderer"
version = "0.1.0"
edition = "2021"

[lib]
name = "sp_renderer"

[dependencies]
sp_qshared = { path = "../qshared" }
sp_engine_qcommon = { path = "../engine/qcommon" }
native_platform = { path = "../../native/platform" }
