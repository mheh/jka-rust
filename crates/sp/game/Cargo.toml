[package]
name = "sp_game"
version = "0.1.0"
edition = "2021"

[lib]
name = "sp_game"
crate-type = ["cdylib", "rlib"]

[dependencies]
sp_qshared = { path = "../qshared" }
sp_bg = { path = "../bg" }
sp_abi = { path = "../abi" }
# STATE-D9 SP mirror: zeroed_box/ZeroValid for GameWorld construction (checkpoint-4 finding).
native_platform = { path = "../../native/platform" }
