[package]
name = "sp_game"
version = "0.1.0"
edition = "2021"

[lib]
name = "sp_game"
crate-type = ["cdylib", "rlib"]

[dependencies]
sp_qshared = { path = "../qshared" }
sp_bg = { path = "../bg" }
sp_abi = { path = "../abi" }
