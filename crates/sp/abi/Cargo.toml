[package]
name = "sp_abi"
version = "0.1.0"
edition = "2021"

[lib]
name = "sp_abi"

[dependencies]
abi_transport = { path = "../../abi-transport" }
native_types = { path = "../../native/types" }
sp_qshared = { path = "../qshared" }
