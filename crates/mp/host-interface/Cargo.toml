[package]
name = "mp_host_interface"
version = "0.1.0"
edition = "2021"

[lib]
name = "mp_host_interface"

[dependencies]
native_math = { path = "../../native/math" }
mp_qshared = { path = "../qshared" }
