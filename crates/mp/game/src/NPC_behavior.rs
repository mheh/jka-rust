// PORT-COMPLETE: NPC_behavior.c 0/21
//! FAITHFUL signature skeleton for `oracle/oracle/codemp/game/NPC_behavior.c`.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep
//! manifest. STAGING ONLY — not wired into crates/. Bodies are
//! `todo!()`; types resolve against already-ported crates, unresolved
//! ones carry `//TODO: Port <type>` markers. Re-run after editing the
//! RULES TABLE in fnskel.py.
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;

// Every function in this file reads/writes the ambient Raven globals `NPC`,
// `NPCInfo`, `ucmd`, `level` (alertEvents/time), and the file-scope
// `enemyVisibility` — none of which are threaded through any parameter here
// (contrast ruling 4/1: EntityId + &mut GameWorld threading). The sibling
// files NPC_combat.c/NPC_AI_Jedi.c hit the identical wall and park under the
// same `ai-context` topic; matching that precedent rather than inventing a
// threading scheme unilaterally.

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo/ucmd globals, no threading
// mechanism resolved.
/// Raven `NPC_BSAdvanceFight`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:29-183`
pub fn NPC_BSAdvanceFight(ctx: GameContext<'_>) {
    todo!("Port NPC_BSAdvanceFight — parked: ai-context")
}

// PORT-ESCALATION(ai-context): body sets `self->think = 0` (fn-ID enum,
// ruling 2) and `self->nextthink` — needs the resolved EntThink enum shape,
// not yet available in this packet.
/// Raven `Disappear`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:185-191`
pub fn Disappear(
    self_: *mut gentity_t,
) {
    todo!("Port Disappear — parked: ai-context")
}

// PORT-ESCALATION(ai-context): body assigns `self->think = Disappear` (fn-ID
// enum, ruling 2) — needs the resolved EntThink enum shape.
/// Raven `BeamOut`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:194-211`
pub fn BeamOut(
    ctx: GameContext<'_>,
    self_: *mut gentity_t,
) {
    todo!("Port BeamOut — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals, no threading
// mechanism resolved.
/// Raven `NPC_BSCinematic`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:213-244`
pub fn NPC_BSCinematic(ctx: GameContext<'_>) {
    todo!("Port NPC_BSCinematic — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals, no threading
// mechanism resolved.
/// Raven `NPC_BSWait`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:246-249`
pub fn NPC_BSWait(ctx: GameContext<'_>) {
    todo!("Port NPC_BSWait — parked: ai-context")
}

// PORT-ESCALATION(ai-context): body is entirely commented out in oracle
// (dead code kept for reference) but still reads ambient NPC/NPCInfo/level —
// no threading mechanism resolved.
/// Raven `NPC_BSInvestigate`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:252-407`
pub fn NPC_BSInvestigate() {
    todo!("Port NPC_BSInvestigate — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals plus `level`
// (alertEvents[], ruling 1) — no threading mechanism resolved.
/// Raven `NPC_CheckInvestigate`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:409-494`
pub fn NPC_CheckInvestigate(
    ctx: GameContext<'_>,
    alertEventNum: c_int,
) -> qboolean {
    todo!("Port NPC_CheckInvestigate — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals, no threading
// mechanism resolved.
/// Raven `NPC_BSSleep`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:500-521`
pub fn NPC_BSSleep(ctx: GameContext<'_>) {
    todo!("Port NPC_BSSleep — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo/ucmd/level globals, no
// threading mechanism resolved.
/// Raven `NPC_BSFollowLeader`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:524-729`
pub fn NPC_BSFollowLeader(ctx: GameContext<'_>) {
    todo!("Port NPC_BSFollowLeader — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals plus `showBBoxes`/
// `NPCDEBUG_BLUE` file-scope externs, no threading mechanism resolved.
/// Raven `NPC_BSJump`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:733-919`
pub fn NPC_BSJump(ctx: GameContext<'_>) {
    todo!("Port NPC_BSJump — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals, no threading
// mechanism resolved.
/// Raven `NPC_BSRemove`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:921-937`
pub fn NPC_BSRemove(ctx: GameContext<'_>) {
    todo!("Port NPC_BSRemove — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo/ucmd/level globals, no
// threading mechanism resolved.
/// Raven `NPC_BSSearch`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:939-1124`
pub fn NPC_BSSearch(ctx: GameContext<'_>) {
    todo!("Port NPC_BSSearch — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPCInfo global, no threading
// mechanism resolved.
/// Raven `NPC_BSSearchStart`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1132-1150`
pub fn NPC_BSSearchStart(
    ctx: GameContext<'_>,
    homeWp: c_int,
    bState: bState_t,
) {
    todo!("Port NPC_BSSearchStart — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo/ucmd globals, no
// threading mechanism resolved.
/// Raven `NPC_BSNoClip`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1160-1191`
pub fn NPC_BSNoClip(ctx: GameContext<'_>) {
    todo!("Port NPC_BSNoClip — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo/ucmd/level globals, no
// threading mechanism resolved.
/// Raven `NPC_BSWander`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1193-1286`
pub fn NPC_BSWander(ctx: GameContext<'_>) {
    todo!("Port NPC_BSWander — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals, no threading
// mechanism resolved.
/// Raven `NPC_Surrender`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1317-1339`
pub fn NPC_Surrender(ctx: GameContext<'_>) {
    todo!("Port NPC_Surrender — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals, no threading
// mechanism resolved.
/// Raven `NPC_CheckSurrender`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1341-1442`
pub fn NPC_CheckSurrender(ctx: GameContext<'_>) -> qboolean {
    todo!("Port NPC_CheckSurrender — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo/ucmd/level globals, no
// threading mechanism resolved.
/// Raven `NPC_BSFlee`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1444-1558`
pub fn NPC_BSFlee(ctx: GameContext<'_>) {
    todo!("Port NPC_BSFlee — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo globals, no threading
// mechanism resolved.
/// Raven `NPC_StartFlee`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1560-1634`
pub fn NPC_StartFlee(
    ctx: GameContext<'_>,
    enemy: *mut gentity_t,
    dangerPoint: vec3_t,
    dangerLevel: c_int,
    fleeTimeMin: c_int,
    fleeTimeMax: c_int,
) {
    todo!("Port NPC_StartFlee — parked: ai-context")
}

// PORT-ESCALATION(ai-context): body reassigns the ambient NPC/NPCInfo
// pointers to `self_`/`self_->NPC` before delegating to `NPC_StartFlee` (same
// escalation) — no threading mechanism resolved.
/// Raven `G_StartFlee`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1636-1648`
pub fn G_StartFlee(
    ctx: GameContext<'_>,
    self_: *mut gentity_t,
    enemy: *mut gentity_t,
    dangerPoint: vec3_t,
    dangerLevel: c_int,
    fleeTimeMin: c_int,
    fleeTimeMax: c_int,
) {
    todo!("Port G_StartFlee — parked: ai-context")
}

// PORT-ESCALATION(ai-context): ambient NPC/NPCInfo/ucmd globals, no
// threading mechanism resolved.
/// Raven `NPC_BSEmplaced`.
///
/// Source: `oracle/oracle/codemp/game/NPC_behavior.c:1650-1748`
pub fn NPC_BSEmplaced(ctx: GameContext<'_>) {
    todo!("Port NPC_BSEmplaced — parked: ai-context")
}
