// PORT-COMPLETE: NPC_senses.c 1/27 (1 ported, 27 parked)
//! FAITHFUL signature skeleton for `oracle/oracle/codemp/game/NPC_senses.c`.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep
//! manifest, then filled by the jampgame mega-pass. STAGING ONLY — not
//! wired into crates/. Most of this file reaches file-scope game state
//! (`level`, the current-NPC `NPC`/`NPCInfo` globals, `g_entities`,
//! `vec3_origin`) or calls `trap_Trace`/`trap_InPVS` (whose resolved
//! wrappers take `&Engine`, which this faithful raw-pointer signature set
//! carries none of) or `CalcEntitySpot`/`vectoangles`/`AngleVectors`/
//! `VectorNormalize` (whose resolved signatures take `vec3_t` out-params
//! by value, so they cannot write results back — see `g_combat.rs`'s
//! established `vec3-outparam-seam` park reason).
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;

/// Raven `G_ClearLineOfSight`.
///
/// Raven: "returns true if can see from point 1 to 2, even through glass (1
/// pane)- doesn't work with portals".
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:11-36`
// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): calls `trap_Trace`
// (resolved wrapper needs `&Engine`) and indexes `g_entities[tr.entityNum]`
// (ruling 1: `GameWorld` owns `g_entities`) — this faithful signature
// carries neither.
pub fn G_ClearLineOfSight(
    point1: vec3_t,
    point2: vec3_t,
    ignore: c_int,
    clipmask: c_int,
) -> qboolean {
    todo!("Port G_ClearLineOfSight — parked: raw-ptr-skeleton-no-world-handle")
}

/// Raven `CanSee`.
///
/// Raven: determine if NPC can see an entity. This is a straight line trace
/// check. This function does not look at PVS or FOV, or take any AI related
/// factors (for example, the NPC's reaction time) into account.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:47-80`
// PORT-ESCALATION(ai-context): reads the ambient `NPC` actor global
// (`b_local.h:63`, ported functions have no threading mechanism resolved for
// it) via `CalcEntitySpot(NPC, ...)`; also calls `trap_Trace` three times
// (resolved wrapper needs `&Engine`, which this signature carries none of)
// and depends on `CalcEntitySpot`'s own unresolved `vec3-outparam-seam`.
pub fn CanSee(
    ent: *mut gentity_t,
) -> qboolean {
    todo!("Port CanSee — parked: ai-context")
}

/// Raven `InFront`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:82-98`
// PORT-ESCALATION(vec3-outparam-seam): calls `VectorNormalize(dir)` and
// `AngleVectors(angles, forward, NULL, NULL)`, whose resolved signatures
// take `vec3_t` out-params by value and so cannot write `dir`/`forward`
// back into this function's locals — the `dot = DotProduct(dir, forward)`
// result would silently use uninitialized data. Same unresolved fork as
// `g_combat.rs`'s established `vec3-outparam-seam` park reason.
pub fn InFront(
    spot: vec3_t,
    from: vec3_t,
    fromAngles: vec3_t,
    threshHold: f32,
) -> qboolean {
    todo!("Port InFront — parked: vec3-outparam-seam (VectorNormalize, AngleVectors)")
}

/// Raven `InFOV3`.
///
/// Raven: IDEA: further off to side of FOV range, higher chance of failing
/// even if technically in FOV, keep core of 50% to sides as always
/// succeeding.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:109-125`
// PORT-ESCALATION(vec3-outparam-seam): calls `vectoangles(deltaVector,
// angles)`, whose resolved signature takes `angles` as a `vec3_t` out-param
// by value and so cannot write it back — the subsequent `AngleDelta`
// comparisons would read uninitialized data. Same unresolved fork as
// `g_combat.rs`'s established `vec3-outparam-seam` park reason.
pub fn InFOV3(
    spot: vec3_t,
    from: vec3_t,
    fromAngles: vec3_t,
    hFOV: c_int,
    vFOV: c_int,
) -> qboolean {
    todo!("Port InFOV3 — parked: vec3-outparam-seam (vectoangles)")
}

/// Raven `InFOV2`.
///
/// Raven: NPC to position.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:129-145`
// PORT-ESCALATION(vec3-outparam-seam): calls `CalcEntitySpot(from,
// SPOT_HEAD, eyes)`, whose resolved signature takes `eyes` as a `vec3_t`
// out-param by value and so cannot write it back before it's forwarded into
// `InFOV3`.
pub fn InFOV2(
    origin: vec3_t,
    from: *mut gentity_t,
    hFOV: c_int,
    vFOV: c_int,
) -> qboolean {
    todo!("Port InFOV2 — parked: vec3-outparam-seam (CalcEntitySpot)")
}

/// Raven `InFOV`.
///
/// Raven: Entity to entity.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:149-208`
// PORT-ESCALATION(vec3-outparam-seam): reads the ambient `vec3_origin`
// global (ruling 1: `GameWorld`-owned, no threading mechanism resolved) via
// `VectorCompare(from->client->renderInfo.eyeAngles, vec3_origin)`; also
// calls `CalcEntitySpot`/`vectoangles`, whose resolved signatures take
// `vec3_t` out-params by value and so cannot write `eyes`/`spot`/`angles`
// back into this function's locals.
pub fn InFOV(
    ent: *mut gentity_t,
    from: *mut gentity_t,
    hFOV: c_int,
    vFOV: c_int,
) -> qboolean {
    todo!("Port InFOV — parked: vec3-outparam-seam (CalcEntitySpot, vectoangles); vec3_origin (seam-threading)")
}

/// Raven `InVisrange`.
///
/// Raven: FIXME: make a calculate visibility for ents that takes into
/// account lighting, movement, turning, crouch/stand up, other anims, hide
/// brushes, etc.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:210-251`
// PORT-ESCALATION(ai-context): reads the ambient `NPC`/`NPCInfo` actor
// globals (`b_local.h:63-64`) directly — `NPCInfo->stats.visrange` and
// `CalcEntitySpot(NPC, ...)` — no threading mechanism resolved for them.
pub fn InVisrange(
    ent: *mut gentity_t,
) -> qboolean {
    todo!("Port InVisrange — parked: ai-context")
}

/// Raven `NPC_CheckVisibility`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:257-325`
// PORT-ESCALATION(ai-context): reads the ambient `NPC`/`NPCInfo` actor
// globals directly (`NPCInfo->stats.hfov`/`vfov`, passed as `NPC` to
// `InFOV`/`CanShoot`); also calls `trap_InPVS` (resolved wrapper needs
// `&Engine`, which this signature carries none of).
pub fn NPC_CheckVisibility(
    ent: *mut gentity_t,
    flags: c_int,
) -> visibility_t {
    todo!("Port NPC_CheckVisibility — parked: ai-context")
}

/// Raven `G_CheckSoundEvents`.
///
/// Raven: NPC_CheckSoundEvents.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:332-386`
// PORT-ESCALATION(seam-threading): iterates `level.alertEvents[0 ..
// level.numAlertEvents]` directly (ruling 1: `GameWorld` owns `level`) — no
// threading mechanism resolved for it.
pub fn G_CheckSoundEvents(
    self_: *mut gentity_t,
    maxHearDist: f32,
    ignoreAlert: c_int,
    mustHaveOwner: qboolean,
    minAlertLevel: c_int,
) -> c_int {
    todo!("Port G_CheckSoundEvents — parked: seam-threading (level)")
}

/// Raven `G_GetLightLevel`.
///
/// Raven: rwwFIXMEFIXME: ...this is evil. We can possibly read from the
/// server BSP data, or load the lightmap along with collision data and
/// whatnot, but is it worth it? Presently a stub returning full brightness.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:388-402`
pub fn G_GetLightLevel(
    pos: vec3_t,
    fromDir: vec3_t,
) -> f32 {
    // rwwFIXMEFIXME: ...this is evil. We can possibly read from the server BSP
    // data, or load the lightmap along with collision data and whatnot, but is
    // it worth it?
    255.0
}

/// Raven `G_CheckSightEvents`.
///
/// Raven: NPC_CheckSightEvents.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:408-468`
// PORT-ESCALATION(seam-threading): iterates `level.alertEvents[0 ..
// level.numAlertEvents]` directly (ruling 1: `GameWorld` owns `level`) — no
// threading mechanism resolved for it.
pub fn G_CheckSightEvents(
    self_: *mut gentity_t,
    hFOV: c_int,
    vFOV: c_int,
    maxSeeDist: f32,
    ignoreAlert: c_int,
    mustHaveOwner: qboolean,
    minAlertLevel: c_int,
) -> c_int {
    todo!("Port G_CheckSightEvents — parked: seam-threading (level)")
}

/// Raven `G_CheckAlertEvents`.
///
/// Raven: NPC_CheckAlertEvents. NOTE: Should all NPCs create alertEvents too
/// so they can detect each other?
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:478-530`
// PORT-ESCALATION(seam-threading): reads `g_entities[0]` and reads/writes
// `level.alertEvents[...]` directly (ruling 1: `GameWorld` owns both) — no
// threading mechanism resolved for them.
pub fn G_CheckAlertEvents(
    self_: *mut gentity_t,
    checkSight: qboolean,
    checkSound: qboolean,
    maxSeeDist: f32,
    maxHearDist: f32,
    ignoreAlert: c_int,
    mustHaveOwner: qboolean,
    minAlertLevel: c_int,
) -> c_int {
    todo!("Port G_CheckAlertEvents — parked: seam-threading (g_entities, level)")
}

/// Raven `NPC_CheckAlertEvents`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:532-535`
// PORT-ESCALATION(ai-context): forwards the ambient `NPC`/`NPCInfo` actor
// globals directly (`NPCInfo->stats.visrange`/`earshot`) into
// `G_CheckAlertEvents` — no threading mechanism resolved for them.
pub fn NPC_CheckAlertEvents(
    checkSight: qboolean,
    checkSound: qboolean,
    ignoreAlert: c_int,
    mustHaveOwner: qboolean,
    minAlertLevel: c_int,
) -> c_int {
    todo!("Port NPC_CheckAlertEvents — parked: ai-context")
}

/// Raven `G_CheckForDanger`.
///
/// Raven: FIXME: more bStates need to call this?
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:537-567`
// PORT-ESCALATION(seam-threading): reads `level.alertEvents[alertEvent]`
// directly (ruling 1: `GameWorld` owns `level`) — no threading mechanism
// resolved for it.
pub fn G_CheckForDanger(
    self_: *mut gentity_t,
    alertEvent: c_int,
) -> qboolean {
    todo!("Port G_CheckForDanger — parked: seam-threading (level)")
}

/// Raven `NPC_CheckForDanger`.
///
/// Raven: FIXME: more bStates need to call this?
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:568-571`
// PORT-ESCALATION(ai-context): forwards the ambient `NPC` actor global
// directly into `G_CheckForDanger` — no threading mechanism resolved for
// it.
pub fn NPC_CheckForDanger(
    alertEvent: c_int,
) -> qboolean {
    todo!("Port NPC_CheckForDanger — parked: ai-context")
}

/// Raven `AddSoundEvent`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:579-615`
// PORT-ESCALATION(seam-threading): reads/writes `level.alertEvents[...]`
// and `level.numAlertEvents`/`curAlertID`/`time` directly (ruling 1:
// `GameWorld` owns `level`) — no threading mechanism resolved for it.
pub fn AddSoundEvent(
    owner: *mut gentity_t,
    position: vec3_t,
    radius: f32,
    alertLevel: alertEventLevel_e,
    needLOS: qboolean,
) {
    todo!("Port AddSoundEvent — parked: seam-threading (level)")
}

/// Raven `AddSightEvent`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:623-652`
// PORT-ESCALATION(seam-threading): reads/writes `level.alertEvents[...]`
// and `level.numAlertEvents`/`curAlertID`/`time` directly (ruling 1:
// `GameWorld` owns `level`) — no threading mechanism resolved for it.
pub fn AddSightEvent(
    owner: *mut gentity_t,
    position: vec3_t,
    radius: f32,
    alertLevel: alertEventLevel_e,
    addLight: f32,
) {
    todo!("Port AddSightEvent — parked: seam-threading (level)")
}

/// Raven `ClearPlayerAlertEvents`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:660-693`
// PORT-ESCALATION(seam-threading): reads/writes `level.alertEvents[...]`/
// `level.numAlertEvents`/`level.time` and the file-static `eventClearTime`
// extern global (ruling 1: `GameWorld` owns both) — no threading mechanism
// resolved for them.
pub fn ClearPlayerAlertEvents() {
    todo!("Port ClearPlayerAlertEvents — parked: seam-threading (level, eventClearTime)")
}

/// Raven `RemoveOldestAlert`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:695-730`
// PORT-ESCALATION(seam-threading): reads/writes `level.alertEvents[...]`/
// `level.numAlertEvents` directly (ruling 1: `GameWorld` owns `level`) — no
// threading mechanism resolved for it.
pub fn RemoveOldestAlert() -> qboolean {
    todo!("Port RemoveOldestAlert — parked: seam-threading (level)")
}

/// Raven `G_ClearLOS`.
///
/// Raven: Position to position.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:739-764`
// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): calls `trap_Trace`
// (resolved wrapper needs `&Engine`) and indexes
// `g_entities[tr.entityNum]` (ruling 1: `GameWorld` owns `g_entities`) —
// this faithful signature carries neither.
pub fn G_ClearLOS(
    self_: *mut gentity_t,
    start: vec3_t,
    end: vec3_t,
) -> qboolean {
    todo!("Port G_ClearLOS — parked: raw-ptr-skeleton-no-world-handle")
}

/// Raven `G_ClearLOS2`.
///
/// Raven: Entity to position.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:767-774`
// PORT-ESCALATION(vec3-outparam-seam): calls `CalcEntitySpot(ent,
// SPOT_HEAD_LEAN, eyes)`, whose resolved signature takes `eyes` as a
// `vec3_t` out-param by value and so cannot write it back before it's
// forwarded into `G_ClearLOS`.
pub fn G_ClearLOS2(
    self_: *mut gentity_t,
    ent: *mut gentity_t,
    end: vec3_t,
) -> qboolean {
    todo!("Port G_ClearLOS2 — parked: vec3-outparam-seam (CalcEntitySpot)")
}

/// Raven `G_ClearLOS3`.
///
/// Raven: Position to entity. Look for the chest first, then the head.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:777-794`
// PORT-ESCALATION(vec3-outparam-seam): calls `CalcEntitySpot(ent,
// SPOT_ORIGIN/SPOT_HEAD_LEAN, spot)` twice, whose resolved signature takes
// `spot` as a `vec3_t` out-param by value and so cannot write it back
// before it's forwarded into `G_ClearLOS`.
pub fn G_ClearLOS3(
    self_: *mut gentity_t,
    start: vec3_t,
    ent: *mut gentity_t,
) -> qboolean {
    todo!("Port G_ClearLOS3 — parked: vec3-outparam-seam (CalcEntitySpot)")
}

/// Raven `G_ClearLOS4`.
///
/// Raven: NPC's eyes to entity.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:797-805`
// PORT-ESCALATION(vec3-outparam-seam): calls `CalcEntitySpot(self,
// SPOT_HEAD_LEAN, eyes)`, whose resolved signature takes `eyes` as a
// `vec3_t` out-param by value and so cannot write it back before it's
// forwarded into `G_ClearLOS3`.
pub fn G_ClearLOS4(
    self_: *mut gentity_t,
    ent: *mut gentity_t,
) -> qboolean {
    todo!("Port G_ClearLOS4 — parked: vec3-outparam-seam (CalcEntitySpot)")
}

/// Raven `G_ClearLOS5`.
///
/// Raven: NPC's eyes to position.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:808-816`
// PORT-ESCALATION(vec3-outparam-seam): calls `CalcEntitySpot(self,
// SPOT_HEAD_LEAN, eyes)`, whose resolved signature takes `eyes` as a
// `vec3_t` out-param by value and so cannot write it back before it's
// forwarded into `G_ClearLOS`.
pub fn G_ClearLOS5(
    self_: *mut gentity_t,
    end: vec3_t,
) -> qboolean {
    todo!("Port G_ClearLOS5 — parked: vec3-outparam-seam (CalcEntitySpot)")
}

/// Raven `NPC_GetHFOVPercentage`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:824-839`
// PORT-ESCALATION(vec3-outparam-seam): calls `vectoangles(deltaVector,
// angles)`, whose resolved signature takes `angles` as a `vec3_t` out-param
// by value and so cannot write it back — the subsequent `AngleDelta`
// comparison would read uninitialized data.
pub fn NPC_GetHFOVPercentage(
    spot: vec3_t,
    from: vec3_t,
    facing: vec3_t,
    hFOV: f32,
) -> f32 {
    todo!("Port NPC_GetHFOVPercentage — parked: vec3-outparam-seam (vectoangles)")
}

/// Raven `NPC_GetVFOVPercentage`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:847-862`
// PORT-ESCALATION(vec3-outparam-seam): calls `vectoangles(deltaVector,
// angles)`, whose resolved signature takes `angles` as a `vec3_t` out-param
// by value and so cannot write it back — the subsequent `AngleDelta`
// comparison would read uninitialized data.
pub fn NPC_GetVFOVPercentage(
    spot: vec3_t,
    from: vec3_t,
    facing: vec3_t,
    vFOV: f32,
) -> f32 {
    todo!("Port NPC_GetVFOVPercentage — parked: vec3-outparam-seam (vectoangles)")
}

/// Raven `G_FindLocalInterestPoint`.
///
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:871-907`
// PORT-ESCALATION(seam-threading): iterates `level.interestPoints[0 ..
// level.numInterestPoints]` directly (ruling 1: `GameWorld` owns `level`)
// and calls `trap_InPVS` (resolved wrapper needs `&Engine`, which this
// signature carries none of) — no threading mechanism resolved for either.
pub fn G_FindLocalInterestPoint(
    self_: *mut gentity_t,
) -> c_int {
    todo!("Port G_FindLocalInterestPoint — parked: seam-threading (level, trap_InPVS)")
}

/// Raven `SP_target_interest`.
///
/// Raven: `//QUAKED target_interest (1 0.8 0.5) (-4 -4 -4) (4 4 4)` — a
/// point that a squadmate will look at if standing still. `target` fires
/// when someone looks at this thing. FIXME: rename point_interest.
/// Source: `oracle/oracle/codemp/game/NPC_senses.c:915-934`
// PORT-ESCALATION(seam-threading): reads/writes
// `level.numInterestPoints`/`level.interestPoints[...]` directly (ruling 1:
// `GameWorld` owns `level`) — no threading mechanism resolved for it.
pub fn SP_target_interest(
    self_: *mut gentity_t,
) {
    todo!("Port SP_target_interest — parked: seam-threading (level)")
}
