// PORT-COMPLETE: g_turret_G2.c 0/15
//! FAITHFUL port of `oracle/oracle/codemp/game/g_turret_G2.c`.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep
//! manifest, then filled by the jampgame mega-pass. STAGING ONLY — not
//! wired into crates/. Every function in this file reads `level.time`
//! (directly or through a same-file callee), calls an engine trap
//! (`trap_G2API_*`, `trap_PointContents`, `trap_InPVS`, `trap_Trace`,
//! `trap_LinkEntity`), assigns a *named* function to a `gentity_t`
//! think/use/pain/die field (whose type is the type-port's frozen
//! `Option<unsafe extern "C" fn(...)>`, which the plain `pub fn` shapes
//! the fnskel signatures resolve to cannot satisfy without a trampoline),
//! or references an `EF_*`/`BONE_*` bit-flag constant not yet ported
//! anywhere in the crate graph — none of which the staged raw-pointer
//! signature (no `&Engine`/`GameWorld`/level handle) can reach. See the
//! PORT-ESCALATION marker on each function for its specific blocker.
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time` and
// calls `trap_G2API_SetBoneAngles`, both unreachable from the staged
// raw-pointer signature (no `&Engine`/level handle) — how should globals and
// traps be reached from a raw-pointer skeleton with no threaded world/engine
// param?
/// Raven `G2Tur_SetBoneAngles`.
///
/// Raven: special routine for tracking angles between client and server -rww.
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:24-123`
pub fn G2Tur_SetBoneAngles(
    ent: *mut gentity_t,
    bone: *mut c_char,
    angles: vec3_t,
) {
    todo!("Port G2Tur_SetBoneAngles — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): calls
// `trap_G2API_RemoveGhoul2Model`/`trap_G2API_InitGhoul2Model`/`trap_G2API_AddBolt`
// directly, all unreachable from the staged raw-pointer signature.
/// Raven `turretG2_set_models`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:125-207`
pub fn turretG2_set_models(
    self_: *mut gentity_t,
    dying: qboolean,
) {
    todo!("Port turretG2_set_models — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `self->genericValue8
// < level.time`, unreachable from the staged raw-pointer signature.
/// Raven `TurretG2Pain`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:210-233`
pub fn TurretG2Pain(
    self_: *mut gentity_t,
    attacker: *mut gentity_t,
    damage: c_int,
) {
    todo!("Port TurretG2Pain — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(unported-consts): reads `self->s.eFlags & EF_SHADER_ANIM`
// (`bg_public.h` entity-flag bit) — not yet ported anywhere in the crate
// graph and not in this packet's resolved symbol list.
/// Raven `turretG2_die`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:236-316`
pub fn turretG2_die(
    self_: *mut gentity_t,
    inflictor: *mut gentity_t,
    attacker: *mut gentity_t,
    damage: c_int,
    meansOfDeath: c_int,
) {
    todo!("Port turretG2_die — parked: unported-consts (EF_SHADER_ANIM)")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time` and
// calls `trap_G2API_SetBoneAnim` directly; also references `EF_G2ANIMATING`
// and `BONE_ANIM_OVERRIDE_FREEZE`/`BONE_ANIM_BLEND`, none of which are ported
// yet (unported-consts compounds the same blocker).
/// Raven `TurboLaser_SetBoneAnim`.
///
/// Raven: start an animation on model_root both server side and client side.
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:321-340`
pub fn TurboLaser_SetBoneAnim(
    eweb: *mut gentity_t,
    startFrame: c_int,
    endFrame: c_int,
) {
    todo!("Port TurboLaser_SetBoneAnim — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time` and
// calls `trap_PointContents` directly; also assigns a named function
// (`bolt->think = G_FreeEntity`) to a `gentity_t` fn-pointer field, which the
// frozen `Option<unsafe extern "C" fn(...)>` field type cannot accept from a
// plain `pub fn` without a trampoline (entfield-fnptr-abi-mismatch).
/// Raven `turretG2_fire`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:344-412`
pub fn turretG2_fire(
    ent: *mut gentity_t,
    start: vec3_t,
    dir: vec3_t,
) {
    todo!("Port turretG2_fire — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(entfield-fnptr-abi-mismatch): assigns named functions
// (`self->use = turretG2_base_use`, `self->pain = TurretG2Pain`, `self->die =
// turretG2_die`) to `gentity_t` fields typed
// `Option<unsafe extern "C" fn(...)>` (frozen type port); the fnskel-staged
// bodies are plain `pub fn`, and `ent_fn_enums.rs`'s `EntUse`/`EntPain`/`EntDie`
// dispatch enums are a different field type than what `gentity_t` actually
// carries — which mechanism bridges the two?
/// Raven `turretG2_respawn`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:414-434`
pub fn turretG2_respawn(
    self_: *mut gentity_t,
) {
    todo!("Port turretG2_respawn — parked: entfield-fnptr-abi-mismatch")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time` and
// calls `trap_G2API_GetBoltMatrix` directly.
/// Raven `turretG2_head_think`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:437-482`
pub fn turretG2_head_think(
    self_: *mut gentity_t,
) {
    todo!("Port turretG2_head_think — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time` and
// calls `trap_G2API_GetBoltMatrix` directly.
/// Raven `turretG2_aim`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:485-648`
pub fn turretG2_aim(
    self_: *mut gentity_t,
) {
    todo!("Port turretG2_aim — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time`
// (`self->aimDebounceTime = level.time + 5000`).
/// Raven `turretG2_turnoff`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:651-674`
pub fn turretG2_turnoff(
    self_: *mut gentity_t,
) {
    todo!("Port turretG2_turnoff — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time`
// repeatedly and calls `trap_InPVS`/`trap_Trace` directly.
/// Raven `turretG2_find_enemies`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:677-826`
pub fn turretG2_find_enemies(
    self_: *mut gentity_t,
) -> qboolean {
    todo!("Port turretG2_find_enemies — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes `level.time`
// (`self->nextthink = level.time + FRAMETIME`, `FRAMETIME` itself also
// unported) and calls `trap_InPVS`/`trap_Trace` (via `turretG2_find_enemies`)
// directly.
/// Raven `turretG2_base_think`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:829-956`
pub fn turretG2_base_think(
    self_: *mut gentity_t,
) {
    todo!("Port turretG2_base_think — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(unported-consts): reads `self->s.eFlags & EF_SHADER_ANIM` —
// not yet ported anywhere in the crate graph.
/// Raven `turretG2_base_use`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:959-973`
pub fn turretG2_base_use(
    self_: *mut gentity_t,
    other: *mut gentity_t,
    activator: *mut gentity_t,
) {
    todo!("Port turretG2_base_use — parked: unported-consts (EF_SHADER_ANIM)")
}

// PORT-ESCALATION(unported-consts): writes `base->s.eFlags |=
// EF_SHADER_ANIM`/`EF_RADAROBJECT` — neither ported anywhere in the crate
// graph yet.
/// Raven `SP_misc_turretG2`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:1029-1081`
pub fn SP_misc_turretG2(
    base: *mut gentity_t,
) {
    todo!("Port SP_misc_turretG2 — parked: unported-consts (EF_SHADER_ANIM, EF_RADAROBJECT)")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes
// `level.time`/`g_gametype` and calls `trap_LinkEntity` directly; also
// references several unported consts (`FRAMETIME`, `MAT_METAL`,
// `CONTENTS_BODY|CONTENTS_PLAYERCLIP|CONTENTS_MONSTERCLIP|CONTENTS_SHOTCLIP`,
// `EF2_BRACKET_ENTITY`) and assigns named functions to fn-pointer fields
// (entfield-fnptr-abi-mismatch compounds the same blocker).
/// Raven `finish_spawning_turretG2`.
///
/// Source: `oracle/oracle/codemp/game/g_turret_G2.c:1084-1297`
pub fn finish_spawning_turretG2(
    base: *mut gentity_t,
) {
    todo!("Port finish_spawning_turretG2 — parked: raw-ptr-skeleton-no-world-handle")
}
