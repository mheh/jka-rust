// PORT-COMPLETE: bg_g2_utils.c 0/2
//! FAITHFUL signature skeleton for `oracle/oracle/codemp/game/bg_g2_utils.c`.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep
//! manifest. STAGING ONLY — not wired into crates/. Bodies are
//! `todo!()`; types resolve against already-ported crates, unresolved
//! ones carry `//TODO: Port <type>` markers. Re-run after editing the
//! RULES TABLE in fnskel.py.
//!
//! Both functions in this module are parked: they require G2API trap calls
//! (trap_G2API_AddBolt, trap_G2API_GetBoltMatrix, trap_G2API_GetSurfaceRenderStatus)
//! but their C signatures do not thread an engine context, and the Rust trap
//! wrappers require `&Engine`. This is a systemic gap (see bg_pmove.rs::BG_VehTraceFromCamPos,
//! bg_saga.rs) undecidable from frozen docs without inventing a channel. Escalation
//! needed to decide: add engine context param? Provide a module-scoped engine accessor?
//! Store engine in thread-local or lazy_static?
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;


/// Raven `BG_AttachToRancor`.
///
/// Source: `oracle/oracle/codemp/game/bg_g2_utils.c:25-98`
// PORT-ESCALATION(engine-trap-threading): calls trap_G2API_AddBolt and trap_G2API_GetBoltMatrix
// but C signature threads no engine context for the Rust wrappers.
pub fn BG_AttachToRancor(
    ghoul2: *mut c_void,
    rancYaw: f32,
    rancOrigin: vec3_t,
    time: c_int,
    modelList: *mut qhandle_t,
    modelScale: vec3_t,
    inMouth: qboolean,
    out_origin: vec3_t,
    out_angles: vec3_t,
    out_axis: *mut vec3_t,
) {
    todo!("Port BG_AttachToRancor — parked: engine-trap-threading")
}

/// Raven `BG_GetRootSurfNameWithVariant`.
///
/// Source: `oracle/oracle/codemp/game/bg_g2_utils.c:101-122`
// PORT-ESCALATION(engine-trap-threading): calls trap_G2API_GetSurfaceRenderStatus
// but C signature threads no engine context for the Rust wrapper.
pub fn BG_GetRootSurfNameWithVariant(
    ghoul2: *mut c_void,
    rootSurfName: *const c_char,
    returnSurfName: *mut c_char,
    returnSize: c_int,
) -> qboolean {
    todo!("Port BG_GetRootSurfNameWithVariant — parked: engine-trap-threading")
}
