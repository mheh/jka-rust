// PORT-COMPLETE: g_active.c 0/36
//! FAITHFUL signature skeleton for `oracle/oracle/codemp/game/g_active.c`.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep
//! manifest. STAGING ONLY — not wired into crates/. Bodies are
//! `todo!()`; types resolve against already-ported crates, unresolved
//! ones carry `//TODO: Port <type>` markers. Re-run after editing the
//! RULES TABLE in fnskel.py.
//!
//! PORT STATUS (mega-pass fill phase): all 36 functions PARKED under one
//! systemic topic — `state-threading`. The fnskel signatures take raw
//! `*mut gentity_t` / scalars with NO `GameContext`/`world`/`engine`
//! channel, but faithful bodies pervasively need module-island state
//! (`level`, `g_entities`, `g_clients`, the g_main cvars — all `GameWorld`
//! fields per fork-ruling 1) and the engine-bearing trap seam
//! (`trap::X(engine, …)`), plus non-type Raven constants (`BUTTON_*`,
//! `BOTH_*`, `STAT_*`, `EF_*`, `qtrue`/`qfalse`) the prelude does not
//! re-export. The frozen state-ownership design (STATE-D1 / porting-rules
//! §B3) forbids globals/`static mut`, so a `level` accessor cannot be
//! invented; and the verbatim cross-file signature contract forbids
//! rethreading a `GameContext` param into these signatures (they are
//! called from other files — `ClientThink`/`G_RunClient` off the vmMain
//! dispatch and `g_main`). This is undecidable from the frozen docs +
//! rulings and is escalated rather than fabricated.
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;

/// Raven `P_SetTwitchInfo`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:20-24`
// PORT-ESCALATION(state-threading): raw-pointer skeleton fns have no GameContext/world/engine channel, but faithful bodies need GameWorld state (level/g_entities/cvars) and engine-bearing traps — how should the module island + trap engine handle be threaded into these verbatim signatures without a forbidden global?
pub fn P_SetTwitchInfo(
    ctx: GameContext<'_>,
    client: *mut gclient_t,
) {
    todo!("Port P_SetTwitchInfo — parked: state-threading")
}

/// Raven `P_DamageFeedback`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:36-118`
// PORT-ESCALATION(state-threading): needs level.time + G_AddEvent seam; no world/engine channel in the raw-pointer signature.
pub fn P_DamageFeedback(
    ctx: GameContext<'_>,
    player: *mut gentity_t,
) {
    todo!("Port P_DamageFeedback — parked: state-threading")
}

/// Raven `P_WorldEffects`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:129-205`
// PORT-ESCALATION(state-threading): needs level.time + G_Sound/G_Damage/rand; no world/engine channel.
pub fn P_WorldEffects(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
) {
    todo!("Port P_WorldEffects — parked: state-threading")
}

/// Raven `DoImpact`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:213-405`
// PORT-ESCALATION(state-threading): needs g_gravity cvar, level.time, trap_PointContents (engine); no world/engine channel.
pub fn DoImpact(
    ctx: GameContext<'_>,
    self_: *mut gentity_t,
    other: *mut gentity_t,
    damageSelf: qboolean,
) {
    todo!("Port DoImpact — parked: state-threading")
}

/// Raven `Client_CheckImpactBBrush`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:407-436`
// PORT-ESCALATION(state-threading): needs level.time; no world channel.
pub fn Client_CheckImpactBBrush(
    self_: *mut gentity_t,
    other: *mut gentity_t,
) {
    todo!("Port Client_CheckImpactBBrush — parked: state-threading")
}

/// Raven `G_SetClientSound`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:444-467`
// PORT-ESCALATION(state-threading): needs level.snd_* + level.time; no world channel.
pub fn G_SetClientSound(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
) {
    todo!("Port G_SetClientSound — parked: state-threading")
}

/// Raven `ClientImpacts`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:478-506`
// PORT-ESCALATION(state-threading): needs g_entities[] + entity fn-pointer touch dispatch; no world channel.
pub fn ClientImpacts(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
    pm: *mut pmove_t,
) {
    todo!("Port ClientImpacts — parked: state-threading")
}

/// Raven `G_TouchTriggers`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:516-590`
// PORT-ESCALATION(state-threading): needs g_entities[], level.time, engine traps (EntitiesInBox/EntityContact); no world/engine channel.
pub fn G_TouchTriggers(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
) {
    todo!("Port G_TouchTriggers — parked: state-threading")
}

/// Raven `G_MoverTouchPushTriggers`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:601-671`
// PORT-ESCALATION(state-threading): needs g_entities[] + engine traps; no world/engine channel.
pub fn G_MoverTouchPushTriggers(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
    oldOrg: vec3_t,
) {
    todo!("Port G_MoverTouchPushTriggers — parked: state-threading")
}

/// Raven `SpectatorThink`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:678-740`
// PORT-ESCALATION(state-threading): needs level.time, engine traps, Pmove, g_noSpecMove cvar; no world/engine channel.
pub fn SpectatorThink(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
    ucmd: *mut usercmd_t,
) {
    todo!("Port SpectatorThink — parked: state-threading")
}

/// Raven `ClientInactivityTimer`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:751-774`
// PORT-ESCALATION(state-threading): needs g_inactivity cvar, level, trap_DropClient/SendServerCommand (engine); no world/engine channel.
pub fn ClientInactivityTimer(
    ctx: GameContext<'_>,
    client: *mut gclient_t,
) -> qboolean {
    todo!("Port ClientInactivityTimer — parked: state-threading")
}

/// Raven `ClientTimerActions`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:783-803`
// PORT-ESCALATION(state-threading): entity-only logic but references un-exported STAT_* constants; folded into the same channel/const-availability topic.
pub fn ClientTimerActions(
    ent: *mut gentity_t,
    msec: c_int,
) {
    todo!("Port ClientTimerActions — parked: state-threading")
}

/// Raven `ClientIntermissionThink`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:810-823`
// PORT-ESCALATION(state-threading): entity-only but references un-exported EF_*/BUTTON_* constants; same const-availability facet of the topic.
pub fn ClientIntermissionThink(
    client: *mut gclient_t,
) {
    todo!("Port ClientIntermissionThink — parked: state-threading")
}

/// Raven `G_VehicleAttachDroidUnit`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:826-854`
// PORT-ESCALATION(state-threading): needs level.time + engine G2API traps + NPC_SetAnim; no world/engine channel.
pub fn G_VehicleAttachDroidUnit(
    ctx: GameContext<'_>,
    vehEnt: *mut gentity_t,
) {
    todo!("Port G_VehicleAttachDroidUnit — parked: state-threading")
}

/// Raven `G_CheapWeaponFire`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:857-895`
// PORT-ESCALATION(state-threading): needs g_entities[] + level.time; no world channel.
pub fn G_CheapWeaponFire(
    entNum: c_int,
    ev: c_int,
) {
    todo!("Port G_CheapWeaponFire — parked: state-threading")
}

/// Raven `ClientEvents`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:909-1052`
// PORT-ESCALATION(state-threading): needs level, g_entities[], engine traps; no world/engine channel.
pub fn ClientEvents(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
    oldEventSequence: c_int,
) {
    todo!("Port ClientEvents — parked: state-threading")
}

/// Raven `SendPendingPredictableEvents`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1059-1087`
// PORT-ESCALATION(state-threading): needs g_entities[]/G_AddEvent seam; no world channel.
pub fn SendPendingPredictableEvents(
    ctx: GameContext<'_>,
    ps: *mut playerState_t,
) {
    todo!("Port SendPendingPredictableEvents — parked: state-threading")
}

/// Raven `G_UpdateForceSightBroadcasts`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1103-1147`
// PORT-ESCALATION(state-threading): needs level.numConnectedClients/sortedClients + g_entities[]; no world channel.
pub fn G_UpdateForceSightBroadcasts(
    ctx: GameContext<'_>,
    self_: *mut gentity_t,
) {
    todo!("Port G_UpdateForceSightBroadcasts — parked: state-threading")
}

/// Raven `G_UpdateJediMasterBroadcasts`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1149-1197`
// PORT-ESCALATION(state-threading): needs level.numConnectedClients/sortedClients + g_entities[]; no world channel.
pub fn G_UpdateJediMasterBroadcasts(
    ctx: GameContext<'_>,
    self_: *mut gentity_t,
) {
    todo!("Port G_UpdateJediMasterBroadcasts — parked: state-threading")
}

/// Raven `G_UpdateClientBroadcasts`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1199-1209`
// PORT-ESCALATION(state-threading): entity-only wrapper but delegates to the two parked broadcast fns; parked with them for one atomic integrate.
pub fn G_UpdateClientBroadcasts(
    ctx: GameContext<'_>,
    self_: *mut gentity_t,
) {
    todo!("Port G_UpdateClientBroadcasts — parked: state-threading")
}

/// Raven `G_AddPushVecToUcmd`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1211-1244`
// PORT-ESCALATION(state-threading): needs level.time; no world channel.
pub fn G_AddPushVecToUcmd(
    ctx: GameContext<'_>,
    self_: *mut gentity_t,
    ucmd: *mut usercmd_t,
) {
    todo!("Port G_AddPushVecToUcmd — parked: state-threading")
}

/// Raven `G_StandingAnim`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1246-1258`
// PORT-ESCALATION(state-threading): pure switch on anim, but references un-exported BOTH_STAND* constants + qtrue/qfalse; const-availability facet of the topic.
pub fn G_StandingAnim(
    anim: c_int,
) -> qboolean {
    todo!("Port G_StandingAnim — parked: state-threading")
}

/// Raven `G_ActionButtonPressed`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1260-1300`
// PORT-ESCALATION(state-threading): pure button test, but references un-exported BUTTON_* constants + qtrue/qfalse; const-availability facet of the topic.
pub fn G_ActionButtonPressed(
    buttons: c_int,
) -> qboolean {
    todo!("Port G_ActionButtonPressed — parked: state-threading")
}

/// Raven `G_CheckClientIdle`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1302-1430`
// PORT-ESCALATION(state-threading): needs level.time + cvars; no world channel.
pub fn G_CheckClientIdle(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
    ucmd: *mut usercmd_t,
) {
    todo!("Port G_CheckClientIdle — parked: state-threading")
}

/// Raven `NPC_Accelerate`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1432-1486`
// PORT-ESCALATION(state-threading): needs level.time + NPCInfo state; no world channel.
pub fn NPC_Accelerate(
    ent: *mut gentity_t,
    fullWalkAcc: qboolean,
    fullRunAcc: qboolean,
) {
    todo!("Port NPC_Accelerate — parked: state-threading")
}

/// Raven `NPC_GetWalkSpeed`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1494-1510`
// PORT-ESCALATION(state-threading): reads NPCInfo/stats; references un-exported constants; same channel/const topic.
pub fn NPC_GetWalkSpeed(
    ent: *mut gentity_t,
) -> c_int {
    todo!("Port NPC_GetWalkSpeed — parked: state-threading")
}

/// Raven `NPC_GetRunSpeed`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1517-1573`
// PORT-ESCALATION(state-threading): reads NPCInfo/class stats + constants; same channel/const topic.
pub fn NPC_GetRunSpeed(
    ent: *mut gentity_t,
) -> c_int {
    todo!("Port NPC_GetRunSpeed — parked: state-threading")
}

/// Raven `G_CheckMovingLoopingSounds`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1577-1614`
// PORT-ESCALATION(state-threading): needs level + G_Sound seam; no world channel.
pub fn G_CheckMovingLoopingSounds(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
    ucmd: *mut usercmd_t,
) {
    todo!("Port G_CheckMovingLoopingSounds — parked: state-threading")
}

/// Raven `G_HeldByMonster`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1616-1651`
// PORT-ESCALATION(state-threading): needs g_entities[] via stored pointer→EntityId; no world channel.
pub fn G_HeldByMonster(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
    ucmd: *mut *mut usercmd_t,
) {
    todo!("Port G_HeldByMonster — parked: state-threading")
}

/// Raven `G_SetTauntAnim`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1662-1926`
// PORT-ESCALATION(state-threading): needs level.time + engine traps + G_AddEvent; no world/engine channel.
pub fn G_SetTauntAnim(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
    taunt: c_int,
) {
    todo!("Port G_SetTauntAnim — parked: state-threading")
}

/// Raven `ClientThink_real`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:1939-3611`
// PORT-ESCALATION(state-threading): the 1673-LOC core — pervasively reads/writes level, g_entities[], g_clients, ~dozens of cvars, and the whole engine trap seam, plus multi-entity simultaneous &mut (fork-ruling 4); wholly dependent on the unresolved world/engine channel.
pub fn ClientThink_real(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
) {
    todo!("Port ClientThink_real — parked: state-threading")
}

/// Raven `G_CheckClientTimeouts`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:3620-3640`
// PORT-ESCALATION(state-threading): needs level.time + cvars; no world channel.
pub fn G_CheckClientTimeouts(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
) {
    todo!("Port G_CheckClientTimeouts — parked: state-threading")
}

/// Raven `ClientThink`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:3649-3720`
// PORT-ESCALATION(state-threading): needs g_entities[], level, trap_GetUsercmd (engine); no world/engine channel.
pub fn ClientThink(
    ctx: GameContext<'_>,
    clientNum: c_int,
    ucmd: *mut usercmd_t,
) {
    todo!("Port ClientThink — parked: state-threading")
}

/// Raven `G_RunClient`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:3723-3729`
// PORT-ESCALATION(state-threading): needs level + delegates to ClientThink_real; no world channel.
pub fn G_RunClient(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
) {
    todo!("Port G_RunClient — parked: state-threading")
}

/// Raven `SpectatorClientEndFrame`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:3738-3783`
// PORT-ESCALATION(state-threading): needs level, g_entities[]; no world channel.
pub fn SpectatorClientEndFrame(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
) {
    todo!("Port SpectatorClientEndFrame — parked: state-threading")
}

/// Raven `ClientEndFrame`.
///
/// Source: `oracle/oracle/codemp/game/g_active.c:3794-3874`
// PORT-ESCALATION(state-threading): needs level.time + engine traps (BG_PlayerStateToEntityState/LinkEntity); no world/engine channel.
pub fn ClientEndFrame(
    ctx: GameContext<'_>,
    ent: *mut gentity_t,
) {
    todo!("Port ClientEndFrame — parked: state-threading")
}
