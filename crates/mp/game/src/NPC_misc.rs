// PORT-COMPLETE: NPC_misc.c 0/2

//! FAITHFUL signature skeleton for `oracle/oracle/codemp/game/NPC_misc.c`.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep
//! manifest. STAGING ONLY — not wired into crates/. Bodies are
//! `todo!()`; types resolve against already-ported crates, unresolved
//! ones carry `//TODO: Port <type>` markers. Re-run after editing the
//! RULES TABLE in fnskel.py.
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;


/// Raven `Debug_Printf`.
///
/// Debug logging function that formats a message if the debug level is enabled,
/// then prints it with timestamp prefix.
///
/// Source: `oracle/oracle/codemp/game/NPC_misc.c:10-35`
// PORT-ESCALATION(varargs-seam): C varargs handling at ABI boundary — how to capture
// and use varargs from extern "C" fn with vsprintf, and how to access level.time global
// (currently threaded as GameWorld in porting-rules §B4)?
pub fn Debug_Printf(
    cv: *mut vmCvar_t,
    debugLevel: c_int,
    fmt: *mut c_char,
    // variadic `...` — C varargs, seam decision pending
) {
    todo!("Port Debug_Printf — parked: varargs-seam")
}

/// Raven `Debug_NPCPrintf`.
///
/// Debug logging function similar to Debug_Printf, but adds NPC identification
/// (name and color escape) to the output.
///
/// Source: `oracle/oracle/codemp/game/NPC_misc.c:41-73`
// PORT-ESCALATION(varargs-seam): C varargs handling at ABI boundary — how to capture
// and use varargs from extern "C" fn with vsprintf, and how to access level.time global
// (currently threaded as GameWorld in porting-rules §B4)?
pub fn Debug_NPCPrintf(
    printNPC: *mut gentity_t,
    cv: *mut vmCvar_t,
    debugLevel: c_int,
    fmt: *mut c_char,
    // variadic `...` — C varargs, seam decision pending
) {
    todo!("Port Debug_NPCPrintf — parked: varargs-seam")
}
