// PORT-COMPLETE: g_bot.c 1/25
//! FAITHFUL port of `oracle/oracle/codemp/game/g_bot.c`.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep
//! manifest, then filled by the jampgame mega-pass. STAGING ONLY — not
//! wired into crates/. Nearly every function here reaches file-scope bot
//! state (`g_numBots`/`g_botInfos`/`g_numArenas`/`g_arenaInfos`/
//! `botSpawnQueue`/`bot_minplayers`), `level`/`g_entities`, or an engine
//! trap (`trap_*`) — none of which the staged raw-pointer signatures carry
//! a `GameWorld`/engine handle for. `G_ParseInfos` additionally needs a
//! variadic `va()` call the staged fixed-arity signature can't express.
//! Only `G_GetMapTypeBits` (no globals/traps/varargs) is implemented; the
//! rest are parked. See PORT-ESCALATION markers.
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;
use mp_bg::public::gametype::{
    GT_CTF, GT_CTY, GT_DUEL, GT_FFA, GT_HOLOCRON, GT_JEDIMASTER, GT_POWERDUEL, GT_SIEGE, GT_TEAM,
};
use core::ffi::CStr;

// No libc dependency in this crate: thin `CStr`-based wrapper over the one
// libc call `G_GetMapTypeBits` needs (`strstr`).
//
// # Safety
// `haystack`/`needle` must be valid NUL-terminated C strings.
unsafe fn libc_strstr(haystack: *const c_char, needle: *const c_char) -> *const c_char {
    let hay = CStr::from_ptr(haystack).to_bytes();
    let ndl = CStr::from_ptr(needle).to_bytes();
    if ndl.is_empty() {
        return haystack;
    }
    if let Some(pos) = hay.windows(ndl.len()).position(|w| w == ndl) {
        haystack.add(pos)
    } else {
        core::ptr::null()
    }
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): calls
// `trap_Cvar_VariableStringBuffer`, which needs an `&Engine` the staged
// signature doesn't carry.
/// Raven `trap_Cvar_VariableValue`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:36-41`
pub fn trap_Cvar_VariableValue(
    var_name: *const c_char,
) -> f32 {
    todo!("Port trap_Cvar_VariableValue — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(variadic-c-abi): the tail allocation-size calc calls
// `va("%d", MAX_ARENAS)` — the staged `va` signature is fixed single-arg
// (`format: *const c_char`, no real C varargs), so this can't be threaded
// through faithfully without the variadic-seam decision other porters
// parked the same way (see `Com_Printf`/`COM_ParseError` in this crate).
/// Raven `G_ParseInfos`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:50-99`
pub fn G_ParseInfos(
    buf: *mut c_char,
    max: c_int,
    infos: *mut *mut c_char,
) -> c_int {
    todo!("Port G_ParseInfos — parked: variadic-c-abi")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes
// `g_arenaInfos`/`g_numArenas` (GameWorld-ruling globals) and calls
// `trap_FS_FOpenFile`/`trap_Printf`/`trap_FS_Read`/`trap_FS_FCloseFile`,
// none reachable from the staged signature.
/// Raven `G_LoadArenasFromFile`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:106-127`
pub fn G_LoadArenasFromFile(
    filename: *mut c_char,
) {
    todo!("Port G_LoadArenasFromFile — parked: raw-ptr-skeleton-no-world-handle")
}

/// Raven `G_GetMapTypeBits`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:129-169`
///
/// # Safety
/// `r#type` must be a valid NUL-terminated C string.
pub unsafe fn G_GetMapTypeBits(
    r#type: *mut c_char,
) -> c_int {
    let mut typeBits: c_int = 0;

    if *r#type != 0 {
        let t = r#type as *const c_char;
        if !libc_strstr(t, c"ffa".as_ptr()).is_null() {
            typeBits |= 1 << GT_FFA;
            typeBits |= 1 << GT_TEAM;
        }
        if !libc_strstr(t, c"team".as_ptr()).is_null() {
            typeBits |= 1 << GT_TEAM;
        }
        if !libc_strstr(t, c"holocron".as_ptr()).is_null() {
            typeBits |= 1 << GT_HOLOCRON;
        }
        if !libc_strstr(t, c"jedimaster".as_ptr()).is_null() {
            typeBits |= 1 << GT_JEDIMASTER;
        }
        if !libc_strstr(t, c"duel".as_ptr()).is_null() {
            typeBits |= 1 << GT_DUEL;
            typeBits |= 1 << GT_POWERDUEL;
        }
        if !libc_strstr(t, c"powerduel".as_ptr()).is_null() {
            typeBits |= 1 << GT_DUEL;
            typeBits |= 1 << GT_POWERDUEL;
        }
        if !libc_strstr(t, c"siege".as_ptr()).is_null() {
            typeBits |= 1 << GT_SIEGE;
        }
        if !libc_strstr(t, c"ctf".as_ptr()).is_null() {
            typeBits |= 1 << GT_CTF;
        }
        if !libc_strstr(t, c"cty".as_ptr()).is_null() {
            typeBits |= 1 << GT_CTY;
        }
    } else {
        typeBits |= 1 << GT_FFA;
    }

    typeBits
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_arenaInfos`/
// `g_numArenas` (GameWorld-ruling globals) the staged signature has no
// handle to reach.
/// Raven `G_DoesMapSupportGametype`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:171-213`
pub fn G_DoesMapSupportGametype(
    mapname: *const c_char,
    gametype: c_int,
) -> qboolean {
    todo!("Port G_DoesMapSupportGametype — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_arenaInfos`/
// `g_autoMapCycle`/`g_numArenas` and calls `trap_Cvar_Register`/
// `trap_Cvar_Set`, none reachable from the staged signature.
/// Raven `G_RefreshNextMap`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:216-288`
pub fn G_RefreshNextMap(
    gametype: c_int,
    forced: qboolean,
) -> *const c_char {
    todo!("Port G_RefreshNextMap — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): writes `g_numArenas`,
// reads `g_arenaInfos`/`g_gametype`, and calls `trap_FS_GetFileList`, none
// reachable from the staged (no-arg) signature.
/// Raven `G_LoadArenas`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:295-321`
pub fn G_LoadArenas() {
    todo!("Port G_LoadArenas — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_arenaInfos`/
// `g_numArenas`, unreachable from the staged signature.
/// Raven `G_GetArenaInfoByMap`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:329-339`
pub fn G_GetArenaInfoByMap(
    map: *const c_char,
) -> *const c_char {
    todo!("Port G_GetArenaInfoByMap — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_botInfos`/
// `g_entities`/`g_gametype`/`g_maxclients`/`g_numBots`/`level` and calls
// `trap_Cvar_VariableValue`/`trap_SendConsoleCommand`, none reachable from
// the staged signature; also needs the ruling-3 shared LCG (`random()`) not
// yet threaded here.
/// Raven `G_AddRandomBot`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:373-454`
pub fn G_AddRandomBot(
    team: c_int,
) {
    todo!("Port G_AddRandomBot — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_entities`/
// `g_gametype`/`g_maxclients`/`level` and calls `trap_SendConsoleCommand`,
// none reachable from the staged signature.
/// Raven `G_RemoveRandomBot`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:461-492`
pub fn G_RemoveRandomBot(
    team: c_int,
) -> c_int {
    todo!("Port G_RemoveRandomBot — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_entities`/
// `g_maxclients`/`level`, none reachable from the staged signature.
/// Raven `G_CountHumanPlayers`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:499-518`
pub fn G_CountHumanPlayers(
    team: c_int,
) -> c_int {
    todo!("Port G_CountHumanPlayers — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `botSpawnQueue`/
// `g_entities`/`g_gametype`/`g_maxclients`/`level`, none reachable from the
// staged signature.
/// Raven `G_CountBotPlayers`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:525-562`
pub fn G_CountBotPlayers(
    team: c_int,
) -> c_int {
    todo!("Port G_CountBotPlayers — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `bot_minplayers`/
// `g_gametype`/`g_maxclients`/`level`, calls `trap_Cvar_Update`, and needs
// the fn-scope static `checkminimumplayers_time` (ruling 5: promotes to a
// GameWorld field) — none reachable from the staged (no-arg) signature.
/// Raven `G_CheckMinimumPlayers`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:569-690`
pub fn G_CheckMinimumPlayers() {
    todo!("Port G_CheckMinimumPlayers — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes
// `botSpawnQueue`, reads `level`, unreachable from the staged signature.
/// Raven `G_CheckBotSpawn`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:697-719`
pub fn G_CheckBotSpawn() {
    todo!("Port G_CheckBotSpawn — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes
// `botSpawnQueue`, reads `level`, unreachable from the staged signature.
/// Raven `AddBotToSpawnQueue`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:727-740`
pub fn AddBotToSpawnQueue(
    clientNum: c_int,
    delay: c_int,
) {
    todo!("Port AddBotToSpawnQueue — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): writes `botSpawnQueue`,
// unreachable from the staged signature.
/// Raven `G_RemoveQueuedBotBegin`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:751-760`
pub fn G_RemoveQueuedBotBegin(
    clientNum: c_int,
) {
    todo!("Port G_RemoveQueuedBotBegin — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): calls
// `trap_DropClient`/`trap_GetUserinfo`, needing an `&Engine` the staged
// signature doesn't carry.
/// Raven `G_BotConnect`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:768-784`
pub fn G_BotConnect(
    clientNum: c_int,
    restart: qboolean,
) -> qboolean {
    todo!("Port G_BotConnect — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_entities`/
// `g_gametype` and calls `trap_BotAllocateClient`/`trap_GetUserinfo`/
// `trap_SendServerCommand`/`trap_SetUserinfo`, none reachable from the
// staged signature.
/// Raven `G_AddBot`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:792-1033`
pub fn G_AddBot(
    name: *const c_char,
    skill: f32,
    team: *const c_char,
    delay: c_int,
    altname: *mut c_char,
) {
    todo!("Port G_AddBot — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level` and calls
// `trap_Argv`/`trap_Cvar_VariableIntegerValue`/`trap_Printf`/
// `trap_SendServerCommand`, none reachable from the staged signature.
/// Raven `Svcmd_AddBot_f`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:1041-1093`
pub fn Svcmd_AddBot_f() {
    todo!("Port Svcmd_AddBot_f — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_botInfos`/
// `g_numBots` and calls `trap_Printf`, none reachable from the staged
// signature.
/// Raven `Svcmd_BotList_f`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:1100-1127`
pub fn Svcmd_BotList_f() {
    todo!("Port Svcmd_BotList_f — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): writes `g_botInfos`/
// `g_numBots` and calls `trap_FS_FOpenFile`/`trap_Printf`/`trap_FS_Read`/
// `trap_FS_FCloseFile`, none reachable from the staged signature.
/// Raven `G_LoadBotsFromFile`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:1194-1215`
pub fn G_LoadBotsFromFile(
    filename: *mut c_char,
) {
    todo!("Port G_LoadBotsFromFile — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): writes `g_numBots` and
// calls `trap_Cvar_Register`/`trap_Cvar_VariableIntegerValue`/
// `trap_FS_GetFileList`, none reachable from the staged (no-arg) signature.
/// Raven `G_LoadBots`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:1222-1256`
pub fn G_LoadBots() {
    todo!("Port G_LoadBots — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_botInfos`/
// `g_numBots` and calls `trap_Printf`, none reachable from the staged
// signature.
/// Raven `G_GetBotInfoByNumber`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:1265-1271`
pub fn G_GetBotInfoByNumber(
    num: c_int,
) -> *mut c_char {
    todo!("Port G_GetBotInfoByNumber — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_botInfos`/
// `g_numBots`, unreachable from the staged signature.
/// Raven `G_GetBotInfoByName`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:1279-1291`
pub fn G_GetBotInfoByName(
    name: *const c_char,
) -> *mut c_char {
    todo!("Port G_GetBotInfoByName — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `bot_minplayers`
// and calls `trap_Cvar_Register`, none reachable from the staged signature.
/// Raven `G_InitBots`.
///
/// Source: `oracle/oracle/codemp/game/g_bot.c:1302-1311`
pub fn G_InitBots(
    restart: qboolean,
) {
    todo!("Port G_InitBots — parked: raw-ptr-skeleton-no-world-handle")
}
