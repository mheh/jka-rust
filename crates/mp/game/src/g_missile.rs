// PORT-COMPLETE: g_missile.c 1/9
//! FAITHFUL port of `oracle/oracle/codemp/game/g_missile.c`.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep
//! manifest, then filled by the jampgame mega-pass. STAGING ONLY — not
//! wired into crates/. Functions that reach file-scope game state
//! (`level`, `g_entities`, cvars `d_projectileGhoul2Collision`/
//! `g_g2TraceLod`), an engine trap (`trap_LinkEntity`/`trap_Trace`/
//! `trap_G2Trace`), a fn-pointer *write*/zero-out (ruling 2 — needs an
//! `EntXxx` enum variant and a threaded `GameWorld`/engine handle this
//! raw-pointer signature carries no channel for), or a `vec3_t` out-param
//! whose staged signature passes the (`Copy`) array by value with no
//! write-back channel to the caller, are parked; see PORT-ESCALATION
//! markers at each site (same convention as `g_misc.rs`).
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;
use crate::g_utils::{G_EffectIndex, G_PlayEffectID, G_TempEntity};

use mp_bg::public::entity_event::entity_event_t::EV_SABER_BLOCK;
use mp_bg::weapons::weapon_t::{WP_BLASTER, WP_BOWCASTER, WP_BRYAR_PISTOL};

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_entities` and
// `level.time` (global game state not reachable from this raw-pointer
// signature) and writes the `think`/`nextthink` fn-pointer fields (ruling 2 —
// needs an `EntThink` enum + zero/`None` variant, not a bare `0`).
/// Raven `G_ReflectMissile`.
///
/// Reflect the missile roughly back at it's owner.
/// Source: `oracle/oracle/codemp/game/g_missile.c:24-89`
pub fn G_ReflectMissile(
    ctx: GameContext<'_>,ent: *mut gentity_t, missile: *mut gentity_t, forward: vec3_t) {
    todo!("Port G_ReflectMissile — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time` and
// writes the `think`/`nextthink` fn-pointer fields (ruling 2).
/// Raven `G_DeflectMissile`.
///
/// Source: `oracle/oracle/codemp/game/g_missile.c:91-140`
pub fn G_DeflectMissile(
    ctx: GameContext<'_>,ent: *mut gentity_t, missile: *mut gentity_t, forward: vec3_t) {
    todo!("Port G_DeflectMissile — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time`.
/// Raven `G_BounceMissile`.
///
/// Source: `oracle/oracle/codemp/game/g_missile.c:148-205`
pub fn G_BounceMissile(
    ctx: GameContext<'_>,ent: *mut gentity_t, trace: *mut trace_t) {
    todo!("Port G_BounceMissile — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time`,
// writes `g_entities[ent->r.ownerNum].client->accuracy_hits`, and calls
// `trap_LinkEntity`.
/// Raven `G_ExplodeMissile`.
///
/// Explode a missile without an impact.
/// Source: `oracle/oracle/codemp/game/g_missile.c:215-257`
pub fn G_ExplodeMissile(
    ctx: GameContext<'_>,ent: *mut gentity_t) {
    todo!("Port G_ExplodeMissile — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): indexes `g_entities` and
// reads the shared `vec3_origin` global.
/// Raven `G_RunStuckMissile`.
///
/// Source: `oracle/oracle/codemp/game/g_missile.c:259-277`
pub fn G_RunStuckMissile(
    ctx: GameContext<'_>,ent: *mut gentity_t) {
    todo!("Port G_RunStuckMissile — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(vec3-outparam-seam): `endout` is a C out-param, but the
// staged skeleton signature passes `vec3_t` (`[f32; 3]`, `Copy`) by value with
// no `&mut`/return channel to write it back to the caller — same seam noted
// at `locateCamera` in `g_misc.rs`. Also calls the still-unported
// `VectorNormalize` (q_math.c, in-file skeleton is `todo!()`).
/// Raven `G_BounceProjectile`.
///
/// Source: `oracle/oracle/codemp/game/g_missile.c:284-294`
pub fn G_BounceProjectile(start: vec3_t, impact: vec3_t, dir: vec3_t, endout: vec3_t) {
    todo!("Port G_BounceProjectile — parked: vec3-outparam-seam")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): calls `G_Spawn` (arena
// allocation) and reads `level.time`; writes the `think` fn-pointer field to
// `G_FreeEntity` (ruling 2 — needs `EntThink::G_FreeEntity`, not a bare fn
// pointer store).
/// Raven `CreateMissile`.
///
/// Source: `oracle/oracle/codemp/game/g_missile.c:298-329`
pub fn CreateMissile(
    ctx: GameContext<'_>,
    org: vec3_t,
    dir: vec3_t,
    vel: f32,
    life: c_int,
    owner: *mut gentity_t,
    altFire: qboolean,
) -> *mut gentity_t {
    todo!("Port CreateMissile — parked: raw-ptr-skeleton-no-world-handle")
}

/// Raven `G_MissileBounceEffect`.
///
/// Source: `oracle/oracle/codemp/game/g_missile.c:331-354`
pub fn G_MissileBounceEffect(
    ctx: GameContext<'_>,ent: *mut gentity_t, org: vec3_t, dir: vec3_t) {
    //FIXME: have an EV_BOUNCE_MISSILE event that checks the s.weapon and does the appropriate effect
    unsafe {
        match (*ent).s.weapon {
            WP_BOWCASTER => {
                let fx = G_EffectIndex(c"bowcaster/deflect".as_ptr());
                G_PlayEffectID(fx, (*ent).r.currentOrigin, dir);
            }
            WP_BLASTER | WP_BRYAR_PISTOL => {
                let fx = G_EffectIndex(c"blaster/deflect".as_ptr());
                G_PlayEffectID(fx, (*ent).r.currentOrigin, dir);
            }
            _ => {
                let te = G_TempEntity(ctx, org, EV_SABER_BLOCK as c_int);
                (*te).s.origin = org;
                (*te).s.angles = dir;
                (*te).s.eventParm = 0;
                (*te).s.weapon = 0; //saberNum
                (*te).s.legsAnim = 0; //bladeNum
            }
        }
    }
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time`,
// indexes `g_entities` (owner/otherOwner lookups), and calls
// `trap_LinkEntity`.
/// Raven `G_MissileImpact`.
///
/// Source: `oracle/oracle/codemp/game/g_missile.c:362-801`
pub fn G_MissileImpact(
    ctx: GameContext<'_>,ent: *mut gentity_t, trace: *mut trace_t) {
    todo!("Port G_MissileImpact — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads cvars
// `d_projectileGhoul2Collision`/`g_g2TraceLod`, indexes `g_entities`, reads
// `level.time`, and calls `trap_G2Trace`/`trap_Trace`/`trap_LinkEntity`.
/// Raven `G_RunMissile`.
///
/// Source: `oracle/oracle/codemp/game/g_missile.c:808-1019`
pub fn G_RunMissile(
    ctx: GameContext<'_>,ent: *mut gentity_t) {
    todo!("Port G_RunMissile — parked: raw-ptr-skeleton-no-world-handle")
}
