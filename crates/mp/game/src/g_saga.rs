// PORT-COMPLETE: g_saga.c 5/33
//! FAITHFUL port of `oracle/oracle/codemp/game/g_saga.c` — Siege gametype
//! game-side module.
//!
//! Generated by `tools/closure-prototype/fnskel.py` from the fnsweep manifest,
//! then filled by the jampgame mega-pass. STAGING ONLY — not wired into
//! crates/. Functions that reach file-scope game state (`level`, `g_entities`,
//! the g_saga.c siege globals — `team1`/`team2`/`siege_info`/`siege_valid`/
//! `g_siegePersistant`/`imperial_*`/`rebel_*`/`gSiege*`/`gObjectiveCfgStr`/…),
//! a cvar, or an engine trap cannot be threaded against the staged raw-pointer
//! signatures (no `GameWorld`/engine handle) and are parked. Functions blocked
//! only on unported flag/index `#define`s not present in the crate graph or the
//! packet (`SVF_BROADCAST`, `EF_RADAROBJECT`, `ENTITYNUM_NONE`) are parked too.
//! See PORT-ESCALATION markers.
#![allow(non_snake_case, unused, clippy::all)]

use crate::prelude::*;

use crate::bg_misc::{BG_FindItemForHoldable, BG_FindItemForWeapon};
use crate::bg_saga::BG_SiegeFindThemeForTeam;
use crate::g_client::ClientSpawn;
use crate::g_items::RegisterItem;
use crate::g_main::LogExit;
use crate::g_utils::{G_PlayEffectID, G_SetOrigin, G_TempEntity, G_UseTargets2};
use mp_bg::public::entity_event::entity_event_t;
use mp_bg::public::holdable::HI_NUM_HOLDABLE;
use mp_bg::weapons::weapon_t::WP_NUM_WEAPONS;

// Raven `qboolean` is `c_int`; keep the source spelling at call sites.
// Source: `oracle/oracle/codemp/game/q_shared.h`
const qtrue: qboolean = 1;
const qfalse: qboolean = 0;

/// Raven `G_SiegeRegisterWeaponsAndHoldables` — go through all classes on a
/// team and register their weapons/items for precaching.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:51-87`
pub fn G_SiegeRegisterWeaponsAndHoldables(
    team: c_int,
) {
    unsafe {
        let stm = BG_SiegeFindThemeForTeam(team);

        if !stm.is_null() {
            let mut i = 0;
            while i < (*stm).numClasses {
                let scl = (*stm).classes[i as usize];

                if !scl.is_null() {
                    let mut j = 0;
                    while j < WP_NUM_WEAPONS {
                        if (*scl).weapons & (1 << j) != 0 {
                            // we use this weapon so register it.
                            RegisterItem(BG_FindItemForWeapon(j));
                        }
                        j += 1;
                    }
                    j = 0;
                    while j < HI_NUM_HOLDABLE {
                        if (*scl).invenItems & (1 << j) != 0 {
                            // we use this item so register it.
                            RegisterItem(BG_FindItemForHoldable(j));
                        }
                        j += 1;
                    }
                }
                i += 1;
            }
        }
    }
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): calls `trap_SetConfigstring`,
// which needs the engine handle the staged raw-pointer signature does not carry.
/// Raven `SiegeSetCompleteData` — tell clients this team won for their
/// intermission scoreboard.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:91-94`
pub fn SiegeSetCompleteData(
    team: c_int,
) {
    todo!("Port SiegeSetCompleteData — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes the g_saga.c
// siege globals (`team1`/`team2`/`siege_info`/`siege_valid`/`imperial_*`/…) and
// `level`, `g_gametype`, `g_siege*` cvars, and calls many traps — none reachable
// without the threaded GameWorld/engine handle.
/// Raven `InitSiegeMode`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:96-373`
pub fn InitSiegeMode() {
    todo!("Port InitSiegeMode — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): parses/mutates the
// file-scope `gObjectiveCfgStr` and calls `trap_SetConfigstring` — both need the
// GameWorld/engine handle absent from the staged signature.
/// Raven `G_SiegeSetObjectiveComplete`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:375-427`
pub fn G_SiegeSetObjectiveComplete(
    team: c_int,
    objective: c_int,
    failIt: qboolean,
) {
    todo!("Port G_SiegeSetObjectiveComplete — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads the file-scope
// `gObjectiveCfgStr` global, which becomes a GameWorld field but is unreachable
// from the staged raw-pointer signature.
/// Raven `G_SiegeGetCompletionStatus` — qtrue if objective is currently complete.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:430-480`
pub fn G_SiegeGetCompletionStatus(
    team: c_int,
    objective: c_int,
) -> qboolean {
    todo!("Port G_SiegeGetCompletionStatus — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(fofs-and-variadic-printf): uses `G_Find(t, FOFS(targetname),
// target)` — the FOFS field-offset macro has no ported spelling here — and the
// variadic `G_Printf`; how should the field-offset selector and variadic print
// seam be expressed?
/// Raven `UseSiegeTarget` — use the player who triggered the object to fire the
/// objective's target.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:482-526`
pub fn UseSiegeTarget(
    other: *mut gentity_t,
    en: *mut gentity_t,
    target: *mut c_char,
) {
    todo!("Port UseSiegeTarget — parked: fofs-and-variadic-printf")
}

// PORT-ESCALATION(unported-game-consts): sets `te->r.svFlags |= SVF_BROADCAST`,
// but the `SVF_BROADCAST` svflags `#define` is not ported in the crate graph and
// its value is not in the packet.
/// Raven `SiegeBroadcast_OBJECTIVECOMPLETE`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:528-540`
pub fn SiegeBroadcast_OBJECTIVECOMPLETE(
    team: c_int,
    client: c_int,
    objective: c_int,
) {
    todo!("Port SiegeBroadcast_OBJECTIVECOMPLETE — parked: unported-game-consts")
}

// PORT-ESCALATION(unported-game-consts): sets `te->r.svFlags |= SVF_BROADCAST`;
// the `SVF_BROADCAST` svflags `#define` is unported and its value is not in the
// packet.
/// Raven `SiegeBroadcast_ROUNDOVER`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:542-553`
pub fn SiegeBroadcast_ROUNDOVER(
    winningteam: c_int,
    winningclient: c_int,
) {
    todo!("Port SiegeBroadcast_ROUNDOVER — parked: unported-game-consts")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): indexes the `g_entities`
// global array (and `ENTITYNUM_NONE`); no world handle on the staged signature.
/// Raven `BroadcastObjectiveCompletion`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:555-564`
pub fn BroadcastObjectiveCompletion(
    team: c_int,
    objective: c_int,
    r#final: c_int,
    client: c_int,
) {
    todo!("Port BroadcastObjectiveCompletion — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): walks the `g_entities`
// global array over `MAX_CLIENTS`; no world handle on the staged signature.
/// Raven `AddSiegeWinningTeamPoints`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:566-589`
pub fn AddSiegeWinningTeamPoints(
    team: c_int,
    winner: c_int,
) {
    todo!("Port AddSiegeWinningTeamPoints — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): memsets the file-scope
// `g_siegePersistant` global and calls `trap_SiegePersSet` — both need the
// GameWorld/engine handle.
/// Raven `SiegeClearSwitchData`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:591-595`
pub fn SiegeClearSwitchData() {
    todo!("Port SiegeClearSwitchData — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): walks the `g_entities`
// global over `MAX_CLIENTS`; no world handle on the staged signature.
/// Raven `SiegeDoTeamAssign`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:597-630`
pub fn SiegeDoTeamAssign() {
    todo!("Port SiegeDoTeamAssign — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes the file-scope
// `g_siegePersistant` global and calls `trap_SiegePersGet`/`trap_SiegePersSet`.
/// Raven `SiegeTeamSwitch`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:632-652`
pub fn SiegeTeamSwitch(
    winTeam: c_int,
    winTime: c_int,
) {
    todo!("Port SiegeTeamSwitch — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_entities`,
// `level`, the siege globals (`team1`/`team2`/`siege_info`/`imperial_*`/…), the
// `g_siegeTeamSwitch` cvar, and calls `trap_SetConfigstring` — none reachable.
/// Raven `SiegeRoundComplete`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:654-742`
pub fn SiegeRoundComplete(
    winningteam: c_int,
    winningclient: c_int,
) {
    todo!("Port SiegeRoundComplete — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): indexes the `bgSiegeClasses`
// global class array (a GameWorld/bg field); no world handle on the staged
// signature.
/// Raven `G_ValidateSiegeClassForTeam`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:744-784`
pub fn G_ValidateSiegeClassForTeam(
    ent: *mut gentity_t,
    team: c_int,
) {
    todo!("Port G_ValidateSiegeClassForTeam — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads the `g_gametype` cvar
// and calls `trap_GetUserinfo`/`trap_SetUserinfo` — both need the engine handle.
/// Raven `SetTeamQuick` — bypass most of the normal checks in SetTeam.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:787-834`
pub fn SetTeamQuick(
    ent: *mut gentity_t,
    team: c_int,
    doBegin: qboolean,
) {
    todo!("Port SetTeamQuick — parked: raw-ptr-skeleton-no-world-handle")
}

/// Raven `SiegeRespawn` — respawn a siege player, honoring a pending team switch.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:836-851`
pub fn SiegeRespawn(
    ent: *mut gentity_t,
) {
    unsafe {
        let client = (*ent).client as *mut gclient_t;

        if (*client).sess.sessionTeam != (*client).sess.siegeDesiredTeam {
            SetTeamQuick(ent, (*client).sess.siegeDesiredTeam, qtrue);
        } else {
            ClientSpawn(ent);
            // add a teleportation effect
            let tent = G_TempEntity(
                (*client).ps.origin,
                entity_event_t::EV_PLAYER_TELEPORT_IN as c_int,
            );
            (*tent).s.clientNum = (*ent).s.clientNum;
        }
    }
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): walks the `g_entities`
// global over `MAX_CLIENTS`, reads `level`/`g_preroundState`/`siege_info`, and
// calls `trap_SetConfigstring` — none reachable from the staged signature.
/// Raven `SiegeBeginRound`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:853-903`
pub fn SiegeBeginRound(
    entNum: c_int,
) {
    todo!("Port SiegeBeginRound — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `g_gametype`,
// `level`, `g_entities`, the siege globals/cvars, and calls `trap_SetConfigstring`.
/// Raven `SiegeCheckTimers`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:905-1013`
pub fn SiegeCheckTimers() {
    todo!("Port SiegeCheckTimers — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes the siege goal
// globals (`gSiegeRoundEnded`/`imperial_goals_*`/`rebel_goals_*`); no world
// handle on the staged signature.
/// Raven `SiegeObjectiveCompleted`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1015-1058`
pub fn SiegeObjectiveCompleted(
    team: c_int,
    objective: c_int,
    r#final: c_int,
    client: c_int,
) {
    todo!("Port SiegeObjectiveCompleted — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads the siege globals
// (`siege_valid`/`team1`/`team2`/`siege_info`/`gParseObjectives`) and the
// `EF_RADAROBJECT` eflags const; no world handle on the staged signature.
/// Raven `siegeTriggerUse`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1060-1128`
pub fn siegeTriggerUse(
    ent: *mut gentity_t,
    other: *mut gentity_t,
    activator: *mut gentity_t,
) {
    todo!("Port siegeTriggerUse — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `siege_valid` and the
// `g_gametype` cvar, calls `trap_LinkEntity`, and arms the `use` fn-pointer — no
// world/engine handle on the staged signature.
/// Raven `SP_info_siege_objective`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1137-1179`
pub fn SP_info_siege_objective(
    ent: *mut gentity_t,
) {
    todo!("Port SP_info_siege_objective — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(unported-game-consts): toggles `s.eFlags & EF_RADAROBJECT` and
// `r.svFlags & SVF_BROADCAST`, but neither `#define` is ported in the crate graph
// and their values are not in the packet.
/// Raven `SiegeIconUse` — toggle the radar icon on and off.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1182-1194`
pub fn SiegeIconUse(
    ent: *mut gentity_t,
    other: *mut gentity_t,
    activator: *mut gentity_t,
) {
    todo!("Port SiegeIconUse — parked: unported-game-consts")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `siege_valid`/the
// `g_gametype` cvar and calls `Com_Error`/`trap_LinkEntity` — no world/engine
// handle on the staged signature.
/// Raven `SP_info_siege_radaricon`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1203-1234`
pub fn SP_info_siege_radaricon(
    ent: *mut gentity_t,
) {
    todo!("Port SP_info_siege_radaricon — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads/writes the siege
// globals (`gSiegeRoundEnded`/`team1`/`team2`/`siege_info`/`imperial_goals_*`/
// `rebel_goals_*`); no world handle on the staged signature.
/// Raven `decompTriggerUse`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1236-1291`
pub fn decompTriggerUse(
    ent: *mut gentity_t,
    other: *mut gentity_t,
    activator: *mut gentity_t,
) {
    todo!("Port decompTriggerUse — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `siege_valid`/the
// `g_gametype` cvar and arms the `use` fn-pointer — no world/engine handle on
// the staged signature.
/// Raven `SP_info_siege_decomplete`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1297-1315`
pub fn SP_info_siege_decomplete(
    ent: *mut gentity_t,
) {
    todo!("Port SP_info_siege_decomplete — parked: raw-ptr-skeleton-no-world-handle")
}

/// Raven `siegeEndUse` — do a LogExit for siege when used.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1317-1320`
pub fn siegeEndUse(
    ent: *mut gentity_t,
    other: *mut gentity_t,
    activator: *mut gentity_t,
) {
    LogExit(b"Round ended\0".as_ptr() as *const c_char);
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `siege_valid`/the
// `g_gametype` cvar and arms the `use` fn-pointer — no world/engine handle on
// the staged signature.
/// Raven `SP_target_siege_end`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1325-1334`
pub fn SP_target_siege_end(
    ent: *mut gentity_t,
) {
    todo!("Port SP_target_siege_end — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(unported-game-consts): sets `genericValue8 = ENTITYNUM_NONE`
// and clears `carrier->r.svFlags &= ~SVF_BROADCAST`; neither `#define` is ported
// in the crate graph and their values are not in the packet.
/// Raven `SiegeItemRemoveOwner`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1338-1349`
pub fn SiegeItemRemoveOwner(
    ent: *mut gentity_t,
    carrier: *mut gentity_t,
) {
    todo!("Port SiegeItemRemoveOwner — parked: unported-game-consts")
}

/// Raven `SiegeItemRespawnEffect` — play the objective-item respawn effect and
/// fire its respawn target.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1351-1370`
pub fn SiegeItemRespawnEffect(
    ent: *mut gentity_t,
    newOrg: vec3_t,
) {
    unsafe {
        if !(*ent).target5.is_null() && *(*ent).target5 != 0 {
            G_UseTargets2(ent, ent, (*ent).target5 as *const c_char);
        }

        if (*ent).genericValue10 == 0 {
            // no respawn effect
            return;
        }

        let upAng: vec3_t = [0.0, 0.0, 1.0];

        // Play it once on the current origin, and once on the origin we're
        // respawning to.
        G_PlayEffectID((*ent).genericValue10, (*ent).r.currentOrigin, upAng);
        G_PlayEffectID((*ent).genericValue10, newOrg, upAng);
    }
}

/// Raven `SiegeItemRespawnOnOriginalSpot`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1372-1380`
pub fn SiegeItemRespawnOnOriginalSpot(
    ent: *mut gentity_t,
    carrier: *mut gentity_t,
) {
    unsafe {
        SiegeItemRespawnEffect(ent, (*ent).pos1);
        G_SetOrigin(ent, (*ent).pos1);
        SiegeItemRemoveOwner(ent, carrier);

        // Stop the item from flashing on the radar
        (*ent).s.time2 = 0;
    }
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level` and the
// `g_entities` global array and calls `trap_LinkEntity`/`trap_PointContents` —
// none reachable from the staged raw-pointer signature.
/// Raven `SiegeItemThink`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1382-1475`
pub fn SiegeItemThink(
    ent: *mut gentity_t,
) {
    todo!("Port SiegeItemThink — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads the `gSiegeRoundBegun`
// siege global (plus `ET_NPC`/`PM_SPECTATOR`/`CHAN_AUTO`/`SVF_BROADCAST` consts);
// no world handle on the staged signature.
/// Raven `SiegeItemTouch`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1477-1545`
pub fn SiegeItemTouch(
    self_: *mut gentity_t,
    other: *mut gentity_t,
    trace: *mut trace_t,
) {
    todo!("Port SiegeItemTouch — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time`, which
// becomes a GameWorld field unreachable from the staged raw-pointer signature.
/// Raven `SiegeItemPain` — pulse the radar icon to show the item is under attack.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1547-1551`
pub fn SiegeItemPain(
    self_: *mut gentity_t,
    attacker: *mut gentity_t,
    damage: c_int,
) {
    todo!("Port SiegeItemPain — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time` and arms
// the `think` fn-pointer (`G_FreeEntity`) — both need world/dispatch context
// absent from the staged raw-pointer signature.
/// Raven `SiegeItemDie`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1553-1574`
pub fn SiegeItemDie(
    self_: *mut gentity_t,
    inflictor: *mut gentity_t,
    attacker: *mut gentity_t,
    damage: c_int,
    meansOfDeath: c_int,
) {
    todo!("Port SiegeItemDie — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `level.time`, arms the
// `touch`/`think` fn-pointers, and calls `trap_LinkEntity` — none reachable from
// the staged raw-pointer signature.
/// Raven `SiegeItemUse` — once used, become active.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1576-1623`
pub fn SiegeItemUse(
    ent: *mut gentity_t,
    other: *mut gentity_t,
    activator: *mut gentity_t,
) {
    todo!("Port SiegeItemUse — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): reads `siege_valid`/the
// `g_gametype` cvar/`level`, arms the `pain`/`die`/`use`/`touch`/`think`
// fn-pointers, and calls `trap_LinkEntity` — none reachable from the staged
// raw-pointer signature.
/// Raven `SP_misc_siege_item`.
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1676-1835`
pub fn SP_misc_siege_item(
    ent: *mut gentity_t,
) {
    todo!("Port SP_misc_siege_item — parked: raw-ptr-skeleton-no-world-handle")
}

// PORT-ESCALATION(raw-ptr-skeleton-no-world-handle): walks `g_entities` over
// `level.num_entities`, reads the `weaponData` global, and calls `trap_InPVS`/
// `trap_SendServerCommand` — none reachable from the staged signature.
/// Raven `G_SiegeClientExData` — send extra data about other clients in this
/// client's PVS (support guy, etc.).
///
/// Source: `oracle/oracle/codemp/game/g_saga.c:1845-1886`
pub fn G_SiegeClientExData(
    msgTarg: *mut gentity_t,
) {
    todo!("Port G_SiegeClientExData — parked: raw-ptr-skeleton-no-world-handle")
}
