[package]
name = "mp_ui"
version = "0.1.0"
edition = "2021"

[lib]
name = "mp_ui"
crate-type = ["cdylib", "rlib"]

[dependencies]
mp_qshared = { path = "../qshared" }
mp_bg = { path = "../bg" }
mp_uishared = { path = "../uishared" }
mp_abi = { path = "../abi" }
