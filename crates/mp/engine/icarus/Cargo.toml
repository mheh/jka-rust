[package]
name = "mp_engine_icarus"
version = "0.1.0"
edition = "2021"

[lib]
name = "mp_engine_icarus"

[dependencies]
mp_qshared = { path = "../../qshared" }
native_string = { path = "../../../native/string" }
native_types = { path = "../../../native/types" }
# ruling 56c blanket authorization: §F subsystems depend on the Stage-0 host seam.
mp_host_interface = { path = "../../host-interface" }
