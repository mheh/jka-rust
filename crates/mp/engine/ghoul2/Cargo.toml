[package]
name = "mp_engine_ghoul2"
version = "0.1.0"
edition = "2021"

[lib]
name = "mp_engine_ghoul2"

[dependencies]
mp_qshared = { path = "../../qshared" }
# EngineHost services trait (rulings 11/24/31/33/36; G2SV-D14/G2SV-D15). Every §F
# entry takes `&mut impl EngineHost`; the host-consuming class files `use` it.
mp_host_interface = { path = "../../host-interface" }
