# Goal — pure-Rust dedicated MP engine (`openjkded`)

Build the Rust implementation of the codemp dedicated-server engine: a host
that loads the existing game module dylibs (Rust or Raven-oracle, unmodified)
and answers every syscall byte-identically to the C `openjkded`, verified
differentially at every step.

Scope is total: **every function of the `WinDed.vcproj` Release link set —
110 sources, 2,481 functions, 87,728 LOC — ports 1:1.** No subsystem is
stubbed, deferred, or cut: the VM interpreter and JIT emitter, botlib, ICARUS,
RMG, NPCNav, the null client-stub layer, and the headless renderer
model/shader loader all port. The only boundaries are seams, not deferrals:
the `Sys_*`/`NET_*` platform layer (implemented natively in Rust) and vendored
zlib/png (Rust crates, fixture-gated).

Plan: `docs/plans/2026-07-08-mp-engine-build-out.md`. Port order:
`tools/closure-prototype/out/engine/engine-port-order.{json,tsv,md}`,
generated by `tools/closure-prototype/engineorder.py` — the machine-verified
total dependency order (27 waves; port in `seq` order and no symbol is ever
referenced before it is ported).

## Ground rules (hard, user-directed 2026-07-09)

- [ ] **No `todo!()`, no stub bodies, no `TODO`/`FIXME` markers at any commit
  during the port.** The dependency order makes placeholders unnecessary; a
  porter who cannot finish a function escalates instead of stubbing.
  `grep -rn "TODO\|FIXME\|todo!"` over the engine crates stays empty at every
  commit (existing scaffolding `todo!`s only burn down).
- [ ] **The order comes only from the tool.** `engineorder.py` under its
  pinned parse configuration (plan appendix) is the single source of the port
  order; fix the tool and regenerate, never hand-edit the artifact.
- [ ] **Transcription-first.** Faithful port, parity green, refactor behind
  the passing diff (porting-rules §A2). No safety-idiom conversion during the
  port; the safe-state migration is a separate post-parity phase, as for
  `mp_game`. Structural conventions decided at port time: file-scope globals
  → host-struct fields (never `static mut`); C++-track subsystems (icarus,
  RMG, ghoul2/renderer class internals) take the §F design-first shape;
  `unsafe` confined to the ABI seam.
- [ ] **Every function is verified against the oracle** by at least one of:
  golden fixtures (3a), captured-replay fixtures (3b), or subsystem A/B
  swap-in (3c) — before its subsystem is declared complete.

## Prerequisite (Stage 0)

- [ ] Game-host interface crate: Rust traits transcribing the C seam (syscall
  surface, vmcall driver, shared-memory contract), extracted from the mock
  engine's behavioral spec; `sv_game_system_calls` dispatches it with a real
  ctx (today: `null_mut()` + `todo!` arms).
- [ ] In-repo referee consumes the trait, so each real engine subsystem can
  replace one mock arm under `cargo test`.

## The progression (milestones = wave prefixes; gates in plan §4)

- [ ] **M1 — waves 0–6** (1,974 fns, 80%): util floor, msg/huffman/cm/pk3,
  the 110-function qcommon core knot (one unit), full `null/` layer. Gate:
  oracle goldens + replay fixtures green; FS/cvar/configstring mock arms
  swapped real.
- [ ] **M2 — waves 7–12** (cum 2,399, 97%): the parallel bulk; icarus
  complete. Gate: icarus differential goldens; cm/sv_world seams real under
  the referee.
- [ ] **M3 — waves 13–19** (cum 2,452): renderer, RMG, botlib, ghoul2
  complete. Gate: G2 bone/bolt/collision goldens; `.aas` load parity; every
  mock arm except the server spine replaced.
- [ ] **M4 — waves 20–25** (cum 2,480): `SV_GameSystemCalls` (w20),
  `SV_SpawnServer` (w22), server complete (w25). Gate: every syscall arm is
  real Rust; in-repo referee drives the real spine.
- [ ] **M5 — wave 26**: `Com_Init` — the binary boots. Gate: external
  engine-vs-engine A/B PASS on corpus-ffa1 + corpus-ffa1-combat.

## Acceptance (done means)

- [ ] Pure-Rust `openjkded` boots, loads maps, and runs the game dylib with
  zero divergence against the C engine on the recorded corpora (lockstep A/B,
  per-frame state + syscall-stream digest).
- [ ] The Raven-oracle game dylib ALSO runs unmodified on the Rust engine
  (both dylibs, both engines — the full A/B square).
- [ ] A real network client (`taystjk`) connects and plays on the pure-Rust
  server; the historical prediction-miss class re-tested end-to-end on
  Rust host + Rust game.
- [ ] `grep -rn "TODO\|FIXME\|todo!"` over the engine crates: zero hits.
- [ ] Every ported file carries oracle `Source:` cites per house style; the
  table/vtable-reached bucket (~688 fns) audited per-file, true dead code
  recorded with §20 zero-caller notes instead of speculative ports.

## Explicitly after this process (not in scope)

- Safe-state migration of the engine crates (joins the parked `mp_game`
  migration).
- Client/renderer (non-dedicated) engine halves; SP engine.
- Non-corpora scenario-matrix expansion of the external rig beyond what M5
  requires (tracked in the referee plan).
