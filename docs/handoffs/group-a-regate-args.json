{
  "docs": [
    {
      "path": "docs/architecture/state-ownership.md",
      "title": "State Ownership",
      "prefix": "STATE",
      "scope": "REVISION PASS: revise docs/architecture/state-ownership.md IN PLACE to bake in the 2026-07-02 (post-nap) session resolutions below and close the mechanical gate holes recorded for this doc in docs/handoffs/group-a-regate-findings.json (READ IT — full finding texts, no new design decisions needed for them). Do not rewrite from scratch; same scope as its header.",
      "decisions": [
        "STATE-Q4 RESOLVED (session 2026-07-02, split shape): the ComError payload struct AND a receiverless pub fn com_error(level: errorParm_t, msg: String) -> ! (pure format + panic_any) live in mp_engine_qcommon — callable from every engine crate, including the leaf throw sites (mp_engine_server) this question was about. ALL of Raven's pre-throw engine work moves to the catch side in mp_engine_core (com_frame/com_init recovery), where &mut Engine is in hand: the errorEntered recursion guard and ErrorState bookkeeping (Engine.common.error, set/cleared by the recovery), the ERR_DROP banner print, SV_Shutdown, CL_Disconnect. Observable console-output ORDER is reproduced in the catch to match oracle exactly. A panic raised during recovery escalates to the outer com_init/main catch — Raven's recursive-error ERR_FATAL path. Close STATE-Q4; fix the com_error seam block (receiver removed, home mp_engine_qcommon), the ComError struct-header comment, and the ErrorState master-table row (mutated catch-side by mp_engine_core recovery).",
        "STATE-D6 REFINED (session 2026-07-02, soundness fix — the gate's contested item 0): raw pointer + leaf reborrow. Amend the frozen vmMain sketch: vmMain derives *mut GameWorld from the WorldCell and threads the RAW POINTER through dispatch; &mut GameWorld reborrows happen ONLY at non-overlapping leaf accesses (the same STATE-D1 EntityId reborrow discipline), and any live &mut is dropped before any trap:: call that can re-enter vmMain. Reentrant vmMain entries each derive their own raw pointer — aliasing raw pointers are sound. Rewrite the SAFETY note around single-threaded REENTRANCY (not cross-thread), and drop the incorrect claim that a dispatch-spanning &mut 'aliases the world exactly as Raven's globals did' (true for C globals, UB for Rust &mut). Record as a dated amendment to STATE-D6.",
        "MECHANICAL (close the four holes per the findings file, no forks): (a) VMA citation: sv_game.cpp:401's VMA IS VM_ArgPtr on the default/PC branch (cast-only form is #if __linux__ && __powerpc__); the no-translation conclusion still holds because VM_ArgPtr degenerates to an identity cast for entryPoint-set native-DLL VMs — dataBase stays 0, only assigned in the QVM bytecode-load paths (vm.cpp:444,560); state THAT argument with those cites. (b) Fix the two dangling decision-ID cross-refs: the construction-order note must not cite STATE-D5 (give the g_main.c:979 construction-order content its own labeled note or inline it where cited), and the snd field comment must cite the actual dated snd amendment line (add it to the proper record if missing) instead of 'STATE-D1 amendment'. (c)+(d) Engine.cm gap: state that CollisionWorld has a zeroed/Default empty state mirroring Raven's static zero-initialization of cmg; Engine::new constructs cm: CollisionWorld::default(), CM_LoadMap populates it later; update the master table's constructed-by row accordingly and enumerate cm in the Slice 0 construction description."
      ],
      "outline": "Apply exactly: (1) Rewrite the com_error/ComError seam block per STATE-Q4 (home mp_engine_qcommon, receiverless; catch-side work list in mp_engine_core), close STATE-Q4 in ## Open questions, record the resolution as a dated amendment or new STATE-D record per doc-standards numbering. (2) Amend the frozen vmMain/WorldCell sketch + SAFETY note per the STATE-D6 refinement (raw ptr threading, leaf reborrow discipline, reentrancy-based safety argument). (3) Close the four mechanical holes per the findings file. (4) Keep Status: DRAFT (gates re-stamp).",
      "dossierPath": "docs/dossiers/A2-state-ownership.md",
      "model": "opus"
    },
    {
      "path": "docs/architecture/lifecycle.md",
      "title": "Lifecycle",
      "prefix": "LIFE",
      "scope": "REVISION PASS: revise docs/architecture/lifecycle.md IN PLACE to bake in the 2026-07-02 (post-nap) session resolutions below and close the mechanical gate holes recorded for this doc in docs/handoffs/group-a-regate-findings.json (READ IT — full finding texts; anything genuinely fork-y comes back needsSession, do not guess). Do not rewrite from scratch; same scope as its header.",
      "decisions": [
        "STATE-Q4 RESOLVED (session 2026-07-02, split shape — lifecycle side; state-ownership.md is being amended to match): com_error is receiverless — pub fn com_error(level, msg) -> ! (pure format + panic_any) AND the ComError payload live in mp_engine_qcommon, NOT mp_engine_core. Amend LIFE-D3 (dated amendment): drop the &mut Engine receiver; com_frame/com_init's catch in mp_engine_core performs ALL of Raven's pre-throw work (errorEntered guard + ErrorState bookkeeping, ERR_DROP banner, SV_Shutdown, CL_Disconnect) with oracle-matching print order; recovery-time panic escalates to the outer catch = Raven's recursive ERR_FATAL path. Fix the ComError placement contradiction the gate flagged as a CONFLICT: the body text at ~:578 and the LIFE-D2 amendment text both say core — correct both via dated amendment; the downcast::<ComError>() in core names mp_engine_qcommon::ComError (core already depends on qcommon; 'no cross-crate use' claim is deleted).",
        "CATCH PRINT RESOLVED (session 2026-07-02, faithful literal): com_frame's recovery prints Raven's per-level thrown LITERAL — 'DISCONNECTED\\n' (common.cpp:312), 'DROPPED\\n' (:326), 'NEED CD\\n' (:336) — via the catch (common.cpp:1763), derived from ComError.level; the formatted message is printed only where Raven printed it (the ERR_DROP banner, now emitted catch-side per STATE-Q4). Fix the frozen catch snippet and the faithfulness claims the gate showed contradicting it.",
        "LIFE-Q2 RESOLVED (session 2026-07-02): sys_error relocates INTO mp_engine_core with its frozen signature unchanged, delegating to native/platform fatal primitives (stderr/console print + process exit; the Win32 message-box/console-show surface is deferred to the client-shell slice). No core->app reverse edge; mp/app keeps nothing error-related; no injection machinery on Engine. Close LIFE-Q2.",
        "MECHANICAL (close the six holes per the findings file; use the settled facts above plus LIFE-D4b's base-relative time API and the sibling docs' now-settled records — ModuleRegistry home mp_engine_qcommon, its slice-0 construction empty at Com_Init step 30; Engine.sv is Option<Server> with the ported server_t/serverStatic_t types and Engine.cm is CollisionWorld with a Default empty state constructed by Engine::new per the state-ownership amendment; define/cite the command_line() helper; note SysEventQueue's evPtr ownership on overflow eviction). If a hole cannot be closed without a genuinely new design decision, report it as needsSession rather than inventing."
      ],
      "outline": "Apply exactly: (1) Dated amendments to LIFE-D3 and LIFE-D2's amendment per STATE-Q4 (receiverless com_error in mp_engine_qcommon; catch-side work; ComError home fixed everywhere, including the frozen seam snippets). (2) Fix the catch print to the per-level literal with cites; remove the contradicted faithfulness wording. (3) Record the LIFE-Q2 resolution (sys_error in mp_engine_core over native/platform primitives) and close LIFE-Q2 in ## Open questions; LIFE-Q1 stays open unchanged. (4) Close the six mechanical holes per the findings file. (5) Keep Status: DRAFT (gates re-stamp).",
      "dossierPath": "docs/dossiers/A3-lifecycle.md",
      "model": "opus",
      "depends": ["docs/architecture/state-ownership.md"]
    },
    {
      "path": "docs/architecture/module-loading.md",
      "title": "Module Loading",
      "prefix": "LOAD",
      "scope": "REVISION PASS: revise docs/architecture/module-loading.md IN PLACE to bake in the 2026-07-02 (post-nap) session resolutions below and close the mechanical gate holes recorded for this doc in docs/handoffs/group-a-regate-findings.json (READ IT — full finding texts). Do not rewrite from scratch; same scope as its header.",
      "decisions": [
        "LOAD-Q6 RESOLVED (session 2026-07-02, resolve-at-construction): SearchStep::FsPath carries RESOLVED values — { base: PathBuf, gamedir: String } — not cvar names. ModuleSearchPolicy is built per-load by the caller in mp_engine_qcommon from cvar values (parity with Cvar_VariableString at win_main.cpp:858-860) once the cvar port (B1) lands; slice 0 constructs the policy from hardcoded/CLI paths. sys_load_dll's frozen signature stands unchanged; native/platform stays cvar-free. Search-order goldens are described over resolved per-platform path sequences. Close LOAD-Q6.",
        "LOAD-Q5 RESOLVED (session 2026-07-02, decide now): new crate crates/mp/engine/wasm-host (package mp_engine_wasm_host) is the home of the host-side wasm types (WasmPtr<T>, ModuleMemory) and isolates the wasmtime dependency; mp_engine_qcommon's ModuleTransport::Wasm arm is feature-gated on it, so native-only builds carry no wasm toolchain. workspace-architecture.md is ALREADY updated (crate tree entry + engine-paragraph note) — cite it, expect no contradiction. Record as a LOAD decision; close LOAD-Q5. Its internals are still designed later (before the first wasm slice) — only the topology is settled.",
        "LOAD-Q7 RESOLVED (session 2026-07-02, follows the STATE-Q4 split shape settled in state-ownership.md/lifecycle.md this same session): ModuleRegistry::load_module reproduces Com_Error(ERR_FATAL) by calling the receiverless mp_engine_qcommon com_error(ERR_FATAL, ...) directly — ModuleRegistry lives in the same crate, exactly Raven-shaped call geometry; the panic unwinds to the mp_engine_core catch (DEC-08 model). load_module keeps its infallible -> SlotId return; the diverging-panic path is noted at the signature. Close LOAD-Q7.",
        "MECHANICAL (close the remaining holes per the findings file, no forks): (a) the LOAD-D6 calling-convention contradiction — the relocated RawSyscall/RawVmMain/RawDllEntry aliases are extern \"C-unwind\" per engine-seam SEAM-D12; the repo's current plain extern \"C\" aliases are swept in the already-tracked SEAM-D12 follow-up slice task, say so with the cross-ref rather than asserting current-repo agreement. (b) Thread RestartKind through the frozen restart signature (ModuleRegistry::restart(&mut self, slot: SlotId, kind: RestartKind) or justify inline why the kind is derived, per the finding). (c) MAX_VM: usize = 3 lives in module_registry.rs alongside ModuleRegistry; state it. (d) State the Cargo wiring: crates/native/platform gains the libloading dependency for loader.rs; mark mechanical."
      ],
      "outline": "Apply exactly: (1) Amend SearchStep::FsPath + the policy-construction story per LOAD-Q6 (resolved values, qcommon construction site, slice-0 source); update the goldens description; close LOAD-Q6. (2) Record the LOAD-Q5 wasm-host resolution citing workspace-architecture.md; close LOAD-Q5. (3) Record the LOAD-Q7 resolution at load_module's frozen signature; close LOAD-Q7. (4) Close the mechanical holes (a)-(d) per the findings file. (5) LOAD-Q1 (macOS filename infix) STAYS open with its owner noted. (6) Keep Status: DRAFT (gates re-stamp).",
      "dossierPath": "docs/dossiers/A4-module-loading.md",
      "model": "opus"
    }
  ],
  "standingDocs": ["docs/doc-standards.md", "docs/porting-rules.md", "docs/workspace-architecture.md", "docs/architecture/two-island-model.md", "docs/architecture/engine-seam.md"]
}
