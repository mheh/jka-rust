{
  "docs": [
    {
      "path": "docs/architecture/engine-seam.md",
      "title": "Engine Seam",
      "prefix": "SEAM",
      "scope": "MINIMAL AMENDMENT + RE-GATE PASS (round 6, stamping round): apply ONLY the user-settled items below (2026-07-03, 'all recommended' resolutions of the round-5 escalations + skeleton checkpoint-3 findings; full record docs/handoffs/2026-07-03-skeleton-findings.md — READ IT), touch nothing else, re-stamp REVIEWED. GATE POLICY: user-sanctioned-open questions with named owners are NOT stamping blockers and must NOT be reported as needsSession. Gate-3 reading set = this doc + standing docs (ALL sibling docs/architecture/*.md included) + docs/abi-traps.md + oracle.",
      "decisions": [
        "GAMECONTEXT CONSTRUCTION RESOLVED (user, 2026-07-03, closes the round-5 cross-doc conflict): pub fields + struct-literal construction WINS — state-ownership.md STATE-D8's frozen block (pub world, pub engine, shell constructs GameContext { world, engine } by literal) is the single frozen home. REMOVE this doc's round-5 'fields are private … exposes pub fn new(...)' sentence (it was a skeleton-artifact, now superseded); the amendment here points at STATE-D8 and both docs must read identically. Rationale recorded: a Copy struct of raw pointers has no invariant a constructor protects; mirrors the WorldPtr precedent.",
        "PINNINGS (user-sanctioned defaults, 2026-07-03 — freeze textually where this doc sketches them): (a) the vmMain dispatch is an INLINE EXHAUSTIVE MATCH directly in vmMain, the exact mirror of the outbound sv_game_system_calls(engine: &mut ServerGame, args: &[isize]) -> isize match — no separate dispatch() helper fn; (b) the outbound trap wrappers live in crates/mp/game/src/trap.rs (one file, mirroring Raven's single g_syscalls.c); (c) the pre-decode bootstrap comparison is spelled `command == MpGameExport::GameInit as c_int` (inline enum-discriminant cast; no standalone c_int constant).",
        "SKELETON CHECKPOINT-3 RECORDS (mechanical, compile/link-forced — findings 10/13/14): (i) SEAM-D12 partial application: game_export_t's fn-pointer fields are flipped to extern \"C-unwind\" NOW (the table cannot hold C-unwind shell fns otherwise); game_import_t untouched, full sweep still pending — record the partial application at SEAM-D12. (ii) SEAM-D10 family line: jagame's OnceLock<game_import_t> is not Sync (raw-pointer member VoiceVolume: *mut c_int) and needs the same local cell wrapper + unsafe impls CEngine got — one line at the SEAM-D10 amendment. (iii) The abi_transport::entrypoints::{qvm, sp_game} stub modules are RETIRED on the skeleton (their #[no_mangle] symbols collide at cdylib link with the live shell exports; per-shell exports already mandated) — record done; cgame/ui carry the stub bodies in their own lib.rs pending their live shape."
      ],
      "outline": "Apply exactly: (1) GameContext amendment — pub-fields/struct-literal resolution, ::new sentence removed, points at STATE-D8. (2) The three pinnings where this doc sketches vmMain/trap/bootstrap. (3) Checkpoint-3 records at SEAM-D12/SEAM-D10/SEAM-D5-or-D10. (4) Nothing else; re-stamp REVIEWED.",
      "dossierPath": "docs/dossiers/A1-engine-seam.md",
      "model": "opus"
    },
    {
      "path": "docs/architecture/state-ownership.md",
      "title": "State Ownership",
      "prefix": "STATE",
      "scope": "REVISION PASS (round 6, stamping round): revise IN PLACE with the user-settled resolutions below (2026-07-03, 'all recommended'; full record docs/handoffs/2026-07-03-skeleton-findings.md — READ IT). GATE POLICY: user-sanctioned-open questions with named owners (STATE-Q2, STATE-Q9) are NOT stamping blockers and must NOT be reported as needsSession. Gate-3 reading set = this doc + standing docs (ALL sibling docs/architecture/*.md included) + docs/abi-traps.md + oracle.",
      "decisions": [
        "GAMECONTEXT CONSTRUCTION RESOLVED (user, 2026-07-03): STATE-D8's frozen block STANDS AS WRITTEN — pub fields, shell constructs by struct literal. Record the dated resolution of the round-5 cross-doc conflict (engine-seam.md's 'private + new()' text is superseded and being removed); strike any ::new reference in this doc. Both docs must read identically after this round.",
        "STATE-Q10 RESOLVED (user, 2026-07-03): zeroed_box becomes zeroed_box<T: ZeroValid>() -> Box<T>, still a SAFE fn, where ZeroValid is a hand-rolled unsafe marker trait in native/platform (bytemuck-Zeroable style, NO new dependency): `unsafe trait ZeroValid` = 'the all-zero bit pattern is a valid value of Self'. Each #[repr(C)] type opts in with a one-line `unsafe impl ZeroValid for X {}` colocated with its layout static-asserts — the per-type audit lives exactly where the layout evidence lives; call sites stay safe; unsafety stays confined per porting-rules D11. Update the STATE-D9 frozen block; close STATE-Q10.",
        "COM_PRINTF RECEIVER CONFIRMED (user, 2026-07-03): STATE-D11 wins — com_printf(&mut Common, ...) living in mp_engine_qcommon (Raven's common.cpp tier; narrowest owner; callers holding &mut Engine reborrow engine.common). lifecycle.md is being amended to match in this same round; update the STATE-Q7 reconciliation note to record this pair as settled.",
        "ENGINE-ISLAND ZEROED CONSTRUCTION SANCTIONED (user, 2026-07-03): Engine::new()'s construction of the large embedded values (sv: Some(Server) with its 1024-entry svEntities, cm: CollisionWorld) REUSES the STATE-D9 zeroed path (now ZeroValid-bounded) by direct analogy to GameWorld — stack-then-move Default is rejected for the same stack-overflow class. The mp_engine_core -> native/platform Cargo edge this needs is sanctioned. Record at the frozen Engine block (lifecycle.md carries the construction mechanics; keep one frozen home, the other points).",
        "STATE-Q8 CLOSED (cites the settled round-4 decision 2026-07-03): keep BOTH Engine names, no renames; always crate-qualify; workspace-architecture.md carries the canonical disambiguation block. Close the question against that record.",
        "PINNINGS + MECHANICAL: (a) Per-file placement gains: the outbound trap wrappers live at crates/mp/game/src/trap.rs (mirrors Raven's single g_syscalls.c). (b) vmMain sketch: dispatch = inline exhaustive match in vmMain (mirror of the outbound match; no helper fn); pre-decode bootstrap comparison spelled `command == MpGameExport::GameInit as c_int`. (c) SP master table: SP GameWorld has NO clients box (SP has no g_clients; MAX_CLIENTS = 1) — divergence preserved by the skeleton, reflect it. (d) Checkpoint-3 finding 15: SP engine-side signatures (sv_init_game_progs, the ge handle placement) are placeholders with no doc home yet — tracked note, owner: the SP engine island pass. (e) STATE-Q9 (SP alias name) stays open, owner: SP slice — sanctioned."
      ],
      "outline": "Apply exactly: (1) GameContext resolution recorded (STATE-D8 stands; ::new struck). (2) STATE-D9 -> ZeroValid form; close STATE-Q10. (3) STATE-D11 confirmed + STATE-Q7 pair settled. (4) Engine-island zeroed construction sanctioned at the frozen Engine block. (5) Close STATE-Q8 vs the round-4 record. (6) Pinnings + mechanical (a)-(e). Gates re-stamp REVIEWED.",
      "dossierPath": "docs/dossiers/A2-state-ownership.md",
      "model": "opus",
      "depends": ["docs/architecture/engine-seam.md"]
    },
    {
      "path": "docs/architecture/module-loading.md",
      "title": "Module Loading",
      "prefix": "LOAD",
      "scope": "MINIMAL AMENDMENT + RE-GATE PASS (round 6, stamping round): apply ONLY the items below (user-settled 2026-07-03; full record docs/handoffs/2026-07-03-skeleton-findings.md — READ IT), touch nothing else, re-stamp REVIEWED. GATE POLICY: LOAD-Q1 and LOAD-Q9 are user-sanctioned-open with named owners — NOT stamping blockers, must NOT be reported as needsSession. Gate-3 reading set = this doc + standing docs (ALL sibling docs/architecture/*.md included) + docs/abi-traps.md + oracle.",
      "decisions": [
        "LOAD-D11 BAD-PARMS GUARD RESOLVED (user, 2026-07-03, closes the round-5 escalation): DROP the !systemCalls disjunct as structurally unreachable — the settled dual (system_calls: SlotSyscall) is a non-nullable Rust fn pointer, exactly the class of the !module disjunct the doc already drops for &str. The guard becomes `if name.is_empty()` only; dated amendment records that Raven's !systemCalls (vm.cpp:480-482) has no reachable Rust dual and that syscall: RawSyscall (Sys_LoadDll's trampoline, a DIFFERENT Raven parameter) is NOT guarded — Raven does not guard it either. Observable behavior identical (the fatal never fires in legitimate use).",
        "MECHANICAL (checkpoint-3 finding 15): where this doc scopes SP, verify it records that SP has NO load_module/EngineSlot/trampoline dual — no VM_Create exists in SP; attach is a direct linked GetGameAPI call (DEC-07 / the settled SP mapping in state-ownership.md). Amend pointer text only if imprecise or missing."
      ],
      "outline": "Apply exactly: (1) LOAD-D11 dated amendment — guard = name.is_empty() only, unreachable-disjunct rationale + cites. (2) SP scoping pointer check. (3) Nothing else; re-stamp REVIEWED.",
      "dossierPath": "docs/dossiers/A4-module-loading.md",
      "model": "opus",
      "depends": ["docs/architecture/engine-seam.md"]
    },
    {
      "path": "docs/architecture/lifecycle.md",
      "title": "Lifecycle",
      "prefix": "LIFE",
      "scope": "MINIMAL AMENDMENT + RE-GATE PASS (round 6, stamping round): apply ONLY the two user-settled items below (2026-07-03; full record docs/handoffs/2026-07-03-skeleton-findings.md — READ IT), touch nothing else, re-stamp REVIEWED. GATE POLICY: LIFE-Q1 is user-sanctioned-open (owner: the pending platform/input subsystem doc) — NOT a stamping blocker, must NOT be reported as needsSession. Gate-3 reading set = this doc + standing docs (ALL sibling docs/architecture/*.md included) + docs/abi-traps.md + oracle.",
      "decisions": [
        "COM_PRINTF RECEIVER RESOLVED (user, 2026-07-03, closes the round-5 STATE-Q7 contradiction): STATE-D11 wins — com_printf takes &mut Common and lives in mp_engine_qcommon. Re-render this doc's frozen block(s) that show com_printf against &mut Engine / mp_engine_core: callers holding &mut Engine reborrow engine.common at the call site. Dated amendment; state-ownership.md records the same pair as settled.",
        "ENGINE::NEW() CONSTRUCTION RESOLVED (user, 2026-07-03, closes the round-5 hole): the large embedded values (sv: Some(Server) — server_t embeds 1024 svEntity_t by value, oracle/oracle/codemp/server/server.h:53-88 — and cm: CollisionWorld) are constructed via the STATE-D9 zeroed path, now zeroed_box<T: ZeroValid> per the STATE-Q10 resolution (see state-ownership.md this round), by direct analogy to GameWorld; stack-then-move Default is REJECTED (same stack-overflow class STATE-D9 exists to prevent). The mp_engine_core -> native/platform Cargo edge is sanctioned. This doc carries the construction mechanics at its Engine::new/Cross-doc-blockers block; state-ownership.md's frozen Engine block points here. If spelling out the mechanics surfaces a genuinely new contradiction (e.g. zero-state semantics of the Option<Server> field), report needsSession rather than inventing."
      ],
      "outline": "Apply exactly: (1) com_printf re-render (&mut Common; reborrow at call sites) as dated amendment. (2) Engine::new zeroed-construction mechanics + sanctioned edge. (3) Nothing else; re-stamp REVIEWED.",
      "dossierPath": "docs/dossiers/A3-lifecycle.md",
      "model": "opus",
      "depends": ["docs/architecture/state-ownership.md"]
    }
  ],
  "standingDocs": ["docs/doc-standards.md", "docs/porting-rules.md", "docs/workspace-architecture.md", "docs/architecture/two-island-model.md", "docs/abi-traps.md", "docs/architecture/engine-seam.md", "docs/architecture/state-ownership.md", "docs/architecture/lifecycle.md", "docs/architecture/module-loading.md", "docs/handoffs/2026-07-03-skeleton-findings.md"]
}
